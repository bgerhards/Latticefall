# Decision log

Append-only. One entry per decision that closed a real question. **Never edit an old
entry** — supersede it with a new one that references it by number.

This file exists so a settled question stays settled. Before proposing a change to
projection, scope, tone, engine, or pipeline: read this first. If the answer is here,
the burden is to show what new information invalidates it, not to re-argue it.

Format: `## NNN — Title` / **Decided** / **Context** / **Rejected** / **Consequence**.

---

## 001 — Godot 4.7.1, GL Compatibility

**Decided.** Ship on Godot 4.7.1 using the GL Compatibility renderer.

**Context.** The project directory already contained a scaffolded Godot 4.7 project with
the MCP editor addon before work began.

**Rejected.** A web stack (TypeScript + PixiJS) would have given a stronger headless
testing story. Not worth discarding existing scaffolding for.

**Consequence.** Compatibility renderer constrains post-processing. This is a reason the
glow architecture (007) is layer-based rather than reliant on engine bloom.

---

## 002 — True 2:1 isometric

**Decided.** Diamond tiles at 2:1. Camera elevation `atan(1/2)` = 26.5651°, orthographic.
Four yaw renders per asset: 45 / 135 / 225 / 315.

**Context.** Chosen over top-down 3/4 with the ~2× asset cost stated up front and accepted.

**Rejected.** Top-down 3/4 orthographic (cheapest, one angle). Side-view lanes (cheapest of
all, wrong genre feel).

**Consequence.** The cost lands on render time rather than authoring time because the
turntable is scripted. Camera angle is now load-bearing for every asset — changing it
invalidates the entire sprite library.

---

## 003 — Power-grid economy as the single core hook

**Decided.** Emplacements have a build cost *and* a continuous MW draw. Reactor capacity is
fixed per anchor. Exceeding it browns the bus out: −40% fire rate across all systems.

**Context.** A game needs one hook, not three. Money is a decision made once and forgotten;
continuous draw is a decision re-made every wave.

**Rejected.** Path-shaping gateways (turns it into maze-TD, and defeats headless balance
verification). Salvage-and-research (a meta layer, leaves moment-to-moment combat generic).

**Consequence.** Power is a scalar over time, so a whole anchor is gradeable headless with
no rendering. This is the single most important property of the design for testability.
Also drives the audio and glow architecture — brownout must be *audible* and *visible*.

---

## 004 — 24 anchors, 3 acts

**Decided.** Three acts of eight. Each act introduces one antagonist, one biome, one power
tier, and one mechanic that invalidates the previous act's dominant strategy.

**Rejected.** 12 anchors (difficulty curve too compressed). 40+ (requires procedural
generation; per-level quality would drop).

**Consequence.** Vertical slice of anchor 01 must be fully polished before anchor 02 exists.

---

## 005 — Grounded military sci-fi

**Decided.** Professionals doing dangerous technical work. Exposition as shop talk. Horror
lands because the people describing it are bored.

**Rejected.** Pulpy adventure (tends to camp in text-only dialog). Cosmic-horror bleak
(fights the arcade feel of tower defense combat).

---

## 006 — Original nomenclature, enforced by a banned list

**Decided.** No terminology from the influencing franchise, anywhere. `docs/NOMENCLATURE.md`
carries a banned table and is the naming authority.

**Consequence.** Every new proper noun is checked against that file before use.

---

## 007 — Glow is a separate layer, never baked into the sprite

**Decided.** Each asset renders twice — albedo with compositing off, glow with compositing
on through a Glare node on the Emission pass. Godot composites the glow additively and
modulates it by bus load.

**Context.** Found while fixing flat-looking emissives. Blender 5.x moved the compositor to
`scene.compositing_node_group` and turned Glare's settings into input sockets.

**Rejected.** Baking bloom into the sprite. It cannot respond to game state, and the core
mechanic *is* power failure — every emissive element must dim during brownout. Baked glow
also bleeds past the alpha silhouette and wrecks atlas packing.

**Consequence.** Two renders per angle, eight per asset. Acceptable; EEVEE is fast.

---

## 008 — Game content is data, validated against schemas

**Decided.** Levels, waves, towers, enemies and dialog are JSON in `data/`, each naming a
schema in `data/schema/`. Code reads data; code does not contain content.

**Consequence.** Balance runs headless. One anchor can be edited without loading the
codebase — the single biggest context-management lever in the project.

---

## 009 — SFX are synthesized, not sampled

**Decided.** 48 of ~60 effects are generated by `tools/audio/synth_sfx.py`. Each is a pure
function of its name, so the bank rebuilds byte-identical anywhere. The remaining ~12
organic sounds are sourced **CC0 only** and logged in `assets/audio/SOURCES.md`.

**Rejected.** CC-BY sourcing — attribution across dozens of clips is a liability nobody
maintains and one missed credit is a licence breach.

**Consequence.** A sound is a diff, not a re-download. UI intervals are shared with the
score: open fifth = affirmative (Meridian), minor second = negative (Ordinal).

---

## 010 — Loudness-match, never peak-normalize

**Decided.** SFX are RMS-matched per category; music is two-pass EBU R128 to per-role
targets (beds −22, combat −18, debrief −24, stingers −17 LUFS).

**Context.** The first SFX bank was peak-normalized and measured RMS 0.24–0.54 — crushed
flat, no crest factor. Post-fix crest is 1.7–4.3.

---

## 011 — Music loops are baked, not engine-managed

**Decided.** Find the tail splice best correlating with the head, bake an equal-power
crossfade. Godot plays the file with loop on and no offset logic.

**Rejected.** An energy-match term in the splice scorer. It was tried, made loops
measurably worse (mean edge delta 1.52 → 3.14 dB), and was reverted.

**Consequence — important.** Automated seam metrics do not describe loop quality here.
Because the crossfade joins samples already adjacent in the source, the seam is continuous
by construction, and windowed energy varies by 10 dB for ordinary musical reasons. **Judge
loops by ear** via `tools/audio/serve.py`. Do not tune the scorer against a seam number.

---

## 012 — Music masters are not versioned

**Decided.** The 14 source WAVs (415 MB) live outside git in `~/Latticefall-masters/`,
verified by SHA-256 recorded in `assets/audio/music_manifest.json`. Only the 28 MB Ogg is
committed. History was rewritten to remove them.

**Context.** 415 MB against a 1 GB free LFS quota, with more music drops expected.

**Consequence.** A fresh clone is ~65 MB rather than ~430 MB. Re-encoding at a different
quality requires restoring masters from backup first.
