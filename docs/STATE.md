# State

*Rewritten at the end of each session. Describes now, for someone with no memory of the
conversation that produced it. The facts below the AUTO marker are regenerated by
`tools/session.py`; everything above it is hand-written. Last updated: 2026-08-04.*

---

## Read this first, before running anything

**Work lands through pull requests, and every PR updates the journal.** Use the `ship` skill:
gate, branch `lf/<epic>-<slug>`, commit with `git commit --only`, **journal**, push, watch CI,
squash-merge, comment the evidence on the issue. The journal step is not optional and is the one
most likely to be skipped. `docs/chronicle/` publishes to
**https://bgerhards.github.io/Latticefall/** on every merge. Eleven PRs landed this way in one
session (#140–#150) and the loop held every time.

**Use the `dispatch` skill to brief every subagent.** It carries the shared-tree rules and the
traps that have actually cost time here.

**Do not write the gate's costs into prose.** The line that used to live here said "`--tier 2`
~23 s" while tier 2 was measured at 30,500 ms in the same file, forty lines down. Run
`tools/check.py --list` for the live check set and `--tier N --json` for today's real cost. What
is stable and worth stating: **a tier is a *minimum*, a tier-excluded check reports `skip` and is
NOT a pass**, and **tier 4 exceeds the 600 s Bash ceiling** whenever the parity digest misses, so
it will background and notify.

**The parity digest covers rule files AND every tracked data file** — `wrap_gate.py` prints the
live count, which is 79 as this is written and will not stay that. So a data-only change forces a
fresh comparison on both platforms (**1,512 runs** since decision 089 added the 21st policy — and
that number moves with the policy set, so read the run's own header rather than this line), and
`wrap_gate.py` will escalate to tier 4 with no watched *path* having changed. That is a digest escalation, not a
path one, and it is correct. **But CI does not agree with the gate about this** (`LF-227`):
`parity-windows.yml` triggers on three rule files only, so a data-only PR gets no Windows leg.

**Pass an explicit timeout to anything long.** The Bash default is 120 s and it does not fail a
slow command, it **backgrounds** it — and a tracked background process re-invokes the model when
it exits, billing a session everyone thought was over.

**Godot captures multiply rather than parallelise** — one takes ~8 of this machine's 16 cores.
Lease-bounded to two machine-wide, so a capture that pauses before starting is waiting for a slot.
A `--script` parity run is only ~2 cores and overlaps freely.

**Only the coordinator runs `tools/reap.py --kill`, and only after every agent has reported.**

## Where the project is

**Latticefall is content-complete at 18×15, and the campaign's own instruments have just been
rebuilt.** 24 anchors grading `ok` at all three difficulties, three acts, a board camera with a
minimap, multi-lane paths, terrain elevation, sixteen-yaw turrets, free placement end to end, CI
running the real gate and parity on both platforms, and a build journal at **69 entries**.

The programme is `docs/PRD-THEATRE-SCALE.md`, decomposed into `docs/issues/`. **Milestones:**
E1 Process 17/20 · E2 Camera **8/8** · E3 Placement **7/7** · E4 Terrain 5/14 · E5 War 6/15 ·
E6 Fidelity 5/8 · E7 Balance 5/7.

**`BAL-04`'s instrument half is now done and its content half is unblocked.** Four decisions
(086–089) replaced the grade-quality selector, confirmed the brownout price, rebased the
acceptance criteria against measurement, and gave the grader the one verb it had never used. The
campaign is graded by 21 policies now, not 20, and the numbers below are that campaign.

## What the last session did

**Four pull requests, #157–#160, four decisions (086–089), and eight backlog items
(`LF-253`–`LF-260`).** The arc is one sentence: **three of the four were measured refusals**, and
every finding came from pointing an instrument at the campaign rather than from auditing the
instrument.

| what was asked | what was found |
|---|---|
| `LF-243`: bound brutal's win share | **no bound exists.** Every *shape* statistic is identical between the healthy campaign and the dissolved one — drop ratio 0.405 vs 0.411, hazard fit 1.143 vs 1.121. Only the *level* differs, and bounding a level takes a fitted constant. Claim weakened until it needed none. |
| `LF-253`: is overdrawing overpowered? | **no — it is an artefact.** The clean/browned split is the same runs at every price. Within policy the effect drops three quarters and flips sign on brutal: +6.0 / +5.2 / **−3.7%**. |
| `LF-255`: is `ROBUST_ENOUGH` misread? | **yes, and three of nine acceptance criteria were failed by the baseline they describe.** One was impossible by construction. |
| `LF-245`: does an upgrade policy help? | **yes on standard, no above it.** 11/24 · 5/24 · **0/24** against `cheap-mass`'s 9 · 9 · 5. |

**#157 — win share, with no constant in it.** The top difficulty's win share must fall strictly
below the bottom one's, on every anchor. 0 of 24 shipped anchors fail; 3 of 24 fail at decision
082's derived ranges. `tools/range_derive.py --shipped-impact` now prints **21/24 clean against
24/24 at the authored ones** where it used to print 24/24 on both sides — the tool that found the
blind spot reports it. `verdict()` is a pure function now, `--selftest` drives all four rules red
and green, and `grade verdict` (tier 1, ~200 ms) is the **red half** of `anchor grades`, proved
red four ways by breaking behaviour. Decision **086**.

**#158 — the discipline gap was a population, not an effect.** Browned-out runs win by 21.8
points raw. Whether a run browns out is decided by the *policy's* build rules, so the raw split
compares policies, not behaviours — the lance is the best weapon and draws the most, so a
lance-led board overdraws. A slope sweep from 1.5 to 6.0 also killed the idea that the price is
the lever: the gap only reaches zero where **five anchors stop grading**, and past slope ~4 the
0.70 cap means **there is no price left to raise**. Decision **087**.

**#159 — the acceptance criteria had never been run against the campaign.** Three of nine failed,
a fourth had been satisfied three sessions earlier by decision 067. `ROBUST_ENOUGH = 8` has now
carried **four** distinct meanings. Every bullet carries its measured baseline and the one-liner
that reproduces it, and the bar is *no anchor gets worse*. Decision **088**.

**#160 — the grader had never bought an upgrade.** 55–62% of every fund is unspent because
`Sim._dispatch_one()` has accepted `upgrade` since BAL-01 and no policy scheduled one.
`upgrade-ladder` is `cheap-mass` plus a schedule, so every difference is attributable to
upgrading alone. It is a **net negative campaign-wide** (16 of 72 cells against 23) that still
opens **nine cells nothing else reaches** and adds a winning build to 10 of 72. Parity ran fresh
on **both** platforms at 1,512 runs, identical. Decision **089**.

**The chronicler found a real error in every one of the four briefs, and two of them had already
reached the tree.** "4 of 24 fail" where a tutorial exemption made it 3; the *paired* run counts
printed beside the *raw* delta under one label; "four of nine bullets failed" where three failed
and a fourth was already satisfied; and "a four-emplacement board beats the five-emplacement one"
when `cheap-mass` wins that cell too. **Brief it to check the brief, and correct in place rather
than quietly** — decisions 087, 088 and 089 each say what they said before review.

## What is broken or missing, in priority order

1. **`BAL-04`'s content half.** Everything its instrument half needed now exists: a selector that
   can see a difficulty dissolve (086), acceptance criteria with baselines (088), and the final
   policy set (089). What remains is the work itself — density targets for 48², the leak budget,
   the capacity ladder, the re-grade and the composition check. **Read the rewritten acceptance
   criteria first**; they are the bar and they now carry the numbers.
2. **`LF-259` — should upgrading die at the top difficulty?** Measured: the ladder wins 0 of 24
   anchors on brutal. The obvious explanation is *wrong* — damage per megawatt improves on
   upgrade (0.75 → 0.78); the loss is in money and probably build sequencing. A design question,
   answered inside `BAL-04`, not a tuning knob.
3. **`LF-218` — acts 1–2 attrit the wave in the first fifth of the lane.** The variable is hp per
   unit (act 1 46, act 2 61, act 3 **123**), not lane length. `BAL-04`'s.
4. **`LF-246` — `coverage.py` reports on vestigial data.** `presence`, `lane`, `site-eff` and
   `verdict()`'s `(a) siting` label are computed over authored slots, which `PLC-04` made
   positionally irrelevant. `LF-185`'s 12.71% → 9.53% result cannot be reproduced and must not be
   cited.
5. **`LF-251` — `game renders` cannot see the board.** Same two numbers across a completely
   different board. Fix: assert emplacement count and distinct ids from the facing dump the
   capture already prints.
6. **`LF-240`/`LF-241` — tier 2 is over budget**, measured at **30,767–30,982 ms** against 28,000
   this session. The fix is still to move `sim determinism` out, not to move the number.
7. **`LF-254` — `hard` does not bite on anchor-04 and anchor-08** (identical win share to
   standard). Reported, not asserted, deliberately — a check red on arrival gets disabled.
8. **`LF-227`** — CI's Windows parity leg still does not run on a data-only change.
9. `LF-242` (`restorer.range` is dead data) · `LF-216` (remove `tile_slot`; owns an atlas repack
   and a re-import — **say so before re-importing**) · `LF-230` (`InstrumentedSim` is 6.8×
   overhead at 78 emplacements) · `LF-249` (200% overlay overlap) · `LF-260` (a policy-list
   name check would turn a 15-minute parity failure into a 10 ms one).

## Needs the owner — do not decide these

**Nothing is blocked.** One thing is worth *telling* rather than asking:

**Upgrading is currently a beginner's option that stops working.** The upgrade-ladder policy wins
11 of 24 anchors on standard, 5 on hard and **0 on brutal**. Whether that is the intended shape
of the mechanic is a genuine design question (`LF-259`) rather than a defect — a mechanic that
fades as difficulty rises is a legitimate choice, and so is one that stays live. It is recorded
rather than decided because `BAL-04`'s re-grade is where it can be answered with the content in
front of it.

*`LF-173`'s asterisk question is unchanged and still small. PRD §6's five decisions all remain
taken.*

## Method that works — do not skip it

- **Look at the thing the check is standing in front of.** Every instrument failure across the
  last two sessions came from that and none from auditing checks. This session added four more —
  a selector blind to a dissolved difficulty, a statistic measuring a population, acceptance
  criteria nobody had run, and a verb no policy had ever dispatched.
- **When two candidate worlds differ only in LEVEL and not in SHAPE, stop looking for the
  constant.** Decision 086: monotonicity, spacing and curvature all passed on the campaign that
  had dissolved. The honest move is to weaken the claim until it needs no constant, not to keep
  hunting for the one that fits.
- **A campaign-wide split of runs by an outcome property compares whatever POPULATIONS that
  property selects.** Decision 087: "browned-out runs win by 21.8 points" was "the policies that
  overdraw are the strong ones". Pair within policy before believing any such split — the
  uncontrolled version even reversed its ordering across difficulties between two campaigns while
  the controlled one stayed near zero in both.
- **Run a criterion against its baseline the day you write it.** Decision 088: three of `BAL-04`'s
  nine acceptance criteria were failed by the campaign they describe, and one was impossible by
  construction.
- **Prove a check red before trusting it green.** `verbs agree` was proved red five ways and
  `playfield width` four; two of `verbs agree`'s modes break *behaviour* rather than an
  expectation, because the other three would pass if the comparator were vacuous.
- **A measured refusal is a successful outcome.** Three of this session's four PRs were one: no
  bound on brutal exists (086), the brownout price is not mispriced and is not the lever anyway
  (087), and `ROBUST_ENOUGH` cannot be read as a floor (088).
- **Brief subagents to check the brief — it has been worth more than any self-review.** Four
  dispatches this session, **four** caught errors in mine, and two had already reached the tree:
  "4 of 24 fail" where a tutorial exemption made it 3; the *paired* run counts printed beside the
  *raw* delta under one label; "four of nine criteria failed" where three did and a fourth was
  already satisfied; and "a four-emplacement board beats the five-emplacement one" when
  `cheap-mass` wins that cell too. **Correct in place, not quietly** — decisions 087, 088 and 089
  each say what they said before review.
- **A number written into a comment is not a number anything checks** — `display_settings.gd` had
  already measured the 4 px playfield and used it only to justify an unrelated default.
- **Author the layout, then sweep, then write dialog** (decision 026). **Check the harness before
  the level.** **Look at UI and art in the engine, never in the source.**

## Traps that have already cost time

Newest first. Full detail in `CLAUDE.md`.

- **`sim/run.py --json` silently drops each report's `runs` unless `--detail` is also passed**, so
  anything computing a per-run statistic off the JSON gets nothing and says nothing. `LF-258`.
- **A grading policy is authored in TWO places that must move together** — `standard_policies()`
  in `sim/engine.py` and `_policies()` in `scripts/test/parity.gd`. Divergence is *not* silent
  (parity names it "present in python, absent from godot") but you learn it 1,512 runs and ~15
  minutes later. `LF-260`.
- **`tools/session.py` re-runs the gate itself** and will blow a 10-minute timeout after a rules
  change. Pass `--gate-from <saved --json artefact>`.
- **`built` entries are `"<tower-id>@<x>,<y>"`, not bare tower ids.** A filter that tests
  membership against a set of tower ids matches nothing and reports a spectacular false zero.
- **After a squash-merge, a stale local `main` blocks `git checkout main`** with a misleading
  "local changes would be overwritten". The fix is `git fetch origin main:main` **while `main` is
  not checked out** — the mirror of the "refusing to fetch into branch 'main' checked out at ..."
  error you get when it is.
- **Backticks inside `tools/backlog.py add "..."` are executed by bash** and silently delete the
  quoted word. It ate two this session. Use single quotes or `-F`.
- **`tools/shot.py --facings` prints the sprite name in field 3, not field 2** — field 2 is the
  drawable class (`tower`), so `awk '{print $2}'` reports one bucket for the whole board.
- **A new untracked file is invisible to the gate**; `git update-index --add` first, then re-run.
- **`git commit --only` cannot add or remove files and fails silently at both.**
- **A check that verifies "output matches input" cannot see a generator that is wrong in a stable
  way.**
- **`scripts/anchor_sim.gd` may never reference an autoload** — 1,152 *empty* parity rows while
  `gdscript parses` stays green.
- **A symptom that matches a known, well-documented, previously-expensive bug is exactly when you
  stop looking.**
- **`git stash` in a shared tree is friendly fire** — eleven files across five workstreams.
- **Rebuilding `.godot/` blanks the level for whoever is playing.** `LF-075`.
- **A GDScript parse error is a hang or a blank frame, never an error at the failure site.** The
  `PostToolUse` guard hook catches these at edit time — it caught an undeclared constant this
  session before it ever reached a run.
- **Density must never be paid for with reactor capacity.**
- Colours are authored in **sRGB**; the sprite pivot is **measured**; ffmpeg here has no
  libvorbis; do not tune the music loop splicer against a seam metric (decision 011).

<!-- BEGIN AUTO — generated by tools/session.py, do not hand-edit -->

*Facts below regenerated 2026-08-04. Run `.venv/bin/python tools/session.py` after any change.*

### Gate

```
[  ok  ] python syntax          1063ms  65 files
[  ok  ] json parses             384ms  87 files
[  ok  ] game data              1385ms  58 documents against 7 schemas (7 exercised) · no warnings
[  ok  ] wave density            126ms  act 1 32 on screen (100%) · act 2 27 on screen (85%) · act 3 21 on screen (66%)
[  ok  ] dialog capacity         228ms  24 briefs quote their own capacity
[  ok  ] banned terms           1140ms  294 files clean
[  ok  ] tier counts               0ms  45 checks, tiers 1-4 all match the docstring
[  ok  ] sfx determinism         954ms  ui_confirm byte-identical
[  ok  ] music manifest           32ms  14 tracks
[  ok  ] sfx loudness           1946ms  86 file(s) measured (86 sfx, 0 music) · ALL WITHIN BAND
[ skip ] music loudness            0ms  excluded by --tier 2 (this check is tier 4) — did not run
[  ok  ] sprite atlas           2432ms  568 cells in 2 pages, in sync
[  ok  ] sprite coverage           8ms  22 ids drawn from 44 sprites
[  ok  ] asset coverage           40ms  22 data ids <-> 26 render assets, both directions clean
[  ok  ] backlog rendered          6ms  91 open
[  ok  ] chronicle current       118ms  69 entries, 0 commit(s) behind
[  ok  ] agent models             20ms  6 agents pinned to opus
[  ok  ] leases wired             26ms  8 launch sites leased
[  ok  ] issue traceability      242ms  ok: no `Closes:` trailer in a7c37734134a7372051480e706f58a1cf4b22e00..HEAD — nothing claimed, nothing to verify
[  ok  ] sim determinism        8525ms  deterministic
[ skip ] anchor grades             0ms  excluded by --tier 2 (this check is tier 3) — did not run
[  ok  ] grade verdict           167ms  sim/run.py selftest: 14 case(s) ok — verdict() fires on 6, stays silent on 4, 4 structural; tiers 'standard' -> 'brutal' read from DIFFICULTIES
[  ok  ] gdscript parses        2179ms  37 scripts parse clean
[  ok  ] godot boots            5110ms  main scene loads clean
[ skip ] scenario smoke            0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] scenario abilities        0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] scenario a11y-worst       0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] scenario lf161-scroll      0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] scenario gamepad          0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] scenario lf226-fallback      0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] save roundtrip            0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] game renders              0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] menu renders              0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] accessibility             0ms  excluded by --tier 2 (this check is tier 3) — did not run
[  ok  ] terrain parsers agree   2460ms  ok — 2 case(s), python and godot agree with the fixture and each other
[ skip ] firing arcs agree         0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] verbs agree               0ms  excluded by --tier 2 (this check is tier 3) — did not run
[ skip ] rules parity              0ms  excluded by --tier 2 (this check is tier 4) — did not run
[ skip ] rules parity (windows)      0ms  excluded by --tier 2 (this check is tier 4) — did not run
[  ok  ] safe operations          36ms  5 files clean (scripts/anchor_sim.gd, sim/engine.py, scripts/test/parity.gd, scripts/test/arc_parity.gd, scripts/test/verb_parity.gd)
[  ok  ] rules autoloads           6ms  clean against 9 autoload(s): Audio, Content, Display, Progress, Recoveries, Sprites, Tuning, Ui, _mcp_game_helper
[  ok  ] yaw hysteresis            3ms  YAW_HYSTERESIS_FRAC=0.25 (< 0.5)
[  ok  ] playfield width           9ms  playfield >= 420px at all 6 scales: 1x 940px, 1.1x 765px, 1.25x 556px, 1.5x 828px (undocked), 1.75x 645px (undocked), 2x 508px (undocked)
[  ok  ] hooks configured       2786ms  4 events wired (PreToolUse, PostToolUse, SubagentStop, SessionEnd) · guard.py --selftest 58/58 ok · +3 more wired (PostCompact, PreCompact, SessionStart), scripts compile
[ skip ] facing harness            0ms  excluded by --tier 2 (this check is tier 3) — did not run

tier 2 — 28 passed · 0 failed · 17 skipped · 31434ms
```

### Inventory

| | count |
|---|---|
| anchors authored | 24 of 24 |
| dialog files | 24 |
| sfx | 86 of ~60 |
| music tracks | 14 of 14 |
| sprite renders | 584 |
| godot scripts | 36 |
| godot scenes | 3 |

### Anchor grades

| anchor | act | cap | waves | standard | hard | brutal | verdict |
|---|---|---|---|---|---|---|---|
| anchor-01 | 1 | 60 MW | 6 | 2/5 | 3/5 | 1/5 | ok |
| anchor-02 | 1 | 80 MW | 7 | 5/10 | 3/10 | 2/11 | ok |
| anchor-03 | 1 | 92 MW | 7 | 4/10 | 4/11 | 2/11 | ok |
| anchor-04 | 1 | 94 MW | 8 | 4/14 | 4/14 | 2/13 | ok |
| anchor-05 | 1 | 100 MW | 7 | 5/15 | 2/12 | 2/13 | ok |
| anchor-06 | 1 | 104 MW | 7 | 6/16 | 5/15 | 3/15 | ok |
| anchor-07 | 1 | 106 MW | 8 | 4/14 | 3/13 | 3/14 | ok |
| anchor-08 | 1 | 110 MW | 9 | 3/15 | 3/14 | 2/14 | ok |
| anchor-09 | 2 | 170 MW | 8 | 8/14 | 2/15 | 2/15 | ok |
| anchor-10 | 2 | 190 MW | 8 | 7/15 | 5/16 | 3/14 | ok |
| anchor-11 | 2 | 200 MW | 8 | 8/16 | 5/16 | 2/15 | ok |
| anchor-12 | 2 | 200 MW | 8 | 9/13 | 5/14 | 5/14 | ok |
| anchor-13 | 2 | 250 MW | 9 | 6/14 | 5/14 | 5/14 | ok |
| anchor-14 | 2 | 250 MW | 9 | 9/14 | 8/14 | 7/14 | ok |
| anchor-15 | 2 | 260 MW | 9 | 7/13 | 6/13 | 5/13 | ok |
| anchor-16 | 2 | 260 MW | 10 | 6/13 | 5/13 | 5/14 | ok |
| anchor-17 | 3 | 290 MW | 8 | 6/15 | 4/15 | 3/15 | ok |
| anchor-18 | 3 | 290 MW | 8 | 7/15 | 4/15 | 3/15 | ok |
| anchor-19 | 3 | 290 MW | 8 | 7/16 | 5/16 | 4/16 | ok |
| anchor-20 | 3 | 290 MW | 8 | 7/16 | 5/16 | 4/16 | ok |
| anchor-21 | 3 | 290 MW | 8 | 4/15 | 3/15 | 4/16 | ok |
| anchor-22 | 3 | 290 MW | 9 | 7/16 | 5/16 | 4/16 | ok |
| anchor-23 | 3 | 290 MW | 9 | 4/15 | 3/15 | 3/15 | ok |
| anchor-24 | 3 | 290 MW | 10 | 7/16 | 5/16 | 4/16 | ok |

*Cells are distinct winning builds / distinct builds tried.*


Campaign win share, pooled over 24 anchors: **standard** 42.4% (142/335) · **hard** 30.6% (102/333) · **brutal** 24.0% (80/334) — falls strictly: yes. Reported, not asserted (decision 086).
`hard` does not fall below `standard` on 2 anchor(s): anchor-04, anchor-08. Only the top tier is a graded problem.

Overdraw advantage — does browning out the bus help? Raw, then controlled for **which policy** overdrew (decision 087; reported, not asserted):

| difficulty | raw | within policy | helps on |
|---|---|---|---|
| standard | +20.0% | +2.6% | 7/19 policies |
| hard | +15.4% | +4.6% | 6/19 policies |
| brutal | +11.9% | -3.5% | 4/19 policies |

### Backlog

91 open · 169 closed

- `high` LF-081 THEATRE SCALE 06. Multi-lane paths and multiple spawn points. The structural blocker: anchor JSON carries a single path array, and anchor_sim.gd and sim/engine.py are both built around one point_at(), one path_length and a leak test of dist >= path_length. Supporting 2-5 simultaneous lanes and up to 4 spawn points is what makes a front line exist at all. Both rule implementations must move together or parity is lost
- `med` LF-059 scripts/main.gd is indented with 4 spaces while every other .gd file uses tabs. Cosmetic, but it makes a whole-file diff out of any tool that normalises indentation
- `med` LF-063 combat_fx.gd/fx_additive.gd failed to parse during a --autoplay --shot anchor-01 sanity run this session ('Cannot infer the type of pos/from/to/screen/d/r variable', then 'Failed to load script ... Parse error' for both files, then 'Invalid call. Nonexistent function bind in base Node2D'), reproduced twice. Both files are unmodified per git diff/status against HEAD, so this was not caused by this session's changes — likely a concurrent agent's in-flight edit or a shared .godot import-cache collision from several agents running Godot on the same working tree at once. Not investigated further: both files are outside this task's ownership (recovery draft feature, scripts/progress.gd + menu.gd + recoveries.gd + draft.gd).
- `med` LF-072 The --autoplay smoke policy (_autobuild_step in anchor_view.gd) fills every free slot with the single highest-priority-affordable tower in Content.unlocked_at() order, so a board never shows a mix of weapon classes -- confirmed while trying to screenshot a mortar/flak burst for the combat-FX pass: anchor-09, anchor-13, anchor-16 and anchor-21 all built out one tower type (arc-node or anchor-damper) and never touched flak-array or mortar-emplacement even many waves in. This is the same root cause LF-053 already names for grading. A '--build <tower-id> <slot-x>,<slot-y>' CLI hook in main.gd would let --shot verify a specific weapon's FX/behaviour on demand without fighting the autobuild policy.
- `med` LF-075 Rebuilding .godot/ blanks the level for anyone playing out of the same tree. The owner plays from D:\dev\Latticefall while agents edit /mnt/d/dev/Latticefall — the same directory. An agent moved .godot aside to force a cold import and the running game lost every imported texture: menu still drew (plain UI), level came up blank. Diagnosed as a code regression for a full pass before the real cause surfaced. Also: an import performed by the Linux Godot build is not sufficient for the Windows editor, which re-imports on open — that was the step that actually restored it. Mitigation is documented in CLAUDE.md; a mechanical version would be a guard in tools/ that refuses to touch .godot while a Godot process is live, or a shot.py flag that imports in place rather than cold
- `med` LF-082 THEATRE SCALE 07. Raise the unit budget to 250-400 alive. Depends on the spatial hash. Also needs the drawables() rebuild reviewed: it allocates and sorts an Array of Dictionaries and is called several times per frame across the view, the glow layer and both FX layers
- `med` LF-083 THEATRE SCALE 08. Unit roles and formations: screens, breakers, siege. Four hundred units walking single file is not an army. Needs units that advance together, shield each other, and behave differently when the lane ahead is blocked
- `med` LF-084 THEATRE SCALE 09. Relay nodes: player-built power generation placed on the board, feeding a radius around themselves. Turns capacity from a number handed down by the brief into ground the player takes and defends. Rule change, so it must be mirrored in sim/engine.py
- `med` LF-085 THEATRE SCALE 10. The pulse: an enemy attack that suppresses rather than damages, taking every emplacement inside a radius offline for a few seconds. The counter is depth and redundancy rather than more damage. Note _covered_by() is a radius test over placed emplacements with no notion of a timed effect ON an emplacement, so this needs a new effect shape in both rule implementations
- `med` LF-087 THEATRE SCALE 12. Siege Battery: multi-tube salvo with a long reload, arcing over the whole board, drawing almost nothing between salvos and enormously during one. The first weapon that spikes the bus rather than loading it, which is a genuinely new shape for the power economy
- `med` LF-088 THEATRE SCALE 13. Cutting Lance: a sustained beam held on one target with damage ramping the longer it holds, so switching targets wastes it. Needs a persistent-beam FX class; the current beam class is a 0.18s snap, not a held state
- `med` LF-089 THEATRE SCALE 14. Spine Driver: charges off spare bus capacity and fires a line that pierces everything in it. The only weapon that rewards staying UNDER capacity, which inverts the usual pressure and is the reason it is worth building
- `med` LF-090 THEATRE SCALE 15. Raise the sprite atlas from 256px to 512px cells and add pages. Four times the pixels per cell; orthographic scale is derived from cell width so the projection stays exact. Measure atlas memory and full-library render time on this machine before committing, since the library will also be growing
- `med` LF-092 THEATRE SCALE 17. Three environment sets with weather and a hazard each, one per act. Each act already owns an antagonist, a power tier and a mechanic; giving it a place with its own light and ground hazard is what makes the campaign feel like it travels
- `med` LF-096 THEATRE SCALE 21. Re-grade the whole campaign at theatre scale. Density targets, leak budgets and the capacity ladder all move with board size, lane count and unit budget; the wave-density gate check will need its act comparison rebased
- `med` LF-097 THEATRE SCALE 22. Performance budget in the gate. A frame-time regression should be a red run rather than a feeling. Needs a fixed-fps timing harness with a per-anchor budget so scaling work cannot silently cost 20ms a frame
- `med` LF-109 tools/shot.py cannot take a menu screenshot and its comment used to claim it could. shot.py unconditionally forwards --anchor and --shot, and menu.gd's _boot_from_cli() tests argv.has('--anchor') or argv.has('--shot') BEFORE it looks at --shot-menu, so --extra --shot-menu is silently dropped and the run screenshots the game instead (confirmed: SHOT/FRAME emitted, MENUFRAME never). The gate's 'menu renders' check reaches it by other means, so this is only a hole in the everyday tool. Fix is a shot.py --menu mode that skips forwarding --anchor/--shot, or a menu.gd precedence change. Found while verifying PRC-09.
- `med` LF-110 CAM-08's real acceptance number was never taken: the issue verifies with tools/shot.py --extra --profile 600, and --profile is CAM-06's deliverable which does not exist in scripts/main.gd yet. The bucketing was proved by a standalone microbenchmark (scripts/test/hit_bucket.gd: per-call cost flat at 2.2-2.9us across 150/300/600/900 units, against a linear reference that climbs 9.3 to 58.1us) plus a 910-query differential test with 0 mismatches. That is the right shape but a different instrument from LF-100's in-engine 7.67ms at 900 units. Re-run the issue's stated command once --profile lands and record the real in-frame number against the 1.0ms target.
- `med` LF-111 The 'whole-frame SHA-256 must match' verification pattern used across several camera and FX issues cannot work on any frame containing combat. combat_fx.gd's sparks, debris and arc jaggedness call randf() unseeded BY DESIGN (a chain node's lightning is meant never to repeat), so two runs of byte-identical source produce different frame hashes: measured 23 differing pixels out of 1166400, one small cluster, max channel delta 279. An agent verifying a presentation-only change against a hash will therefore see a false failure and may go looking for a regression that does not exist. Future issues should specify either a pixel-diff against a repeat-run baseline of the SAME source, or a capture-time RNG seed hook. Found while verifying CAM-08.
- `med` LF-112 The PostToolUse hook that parse-checks an edited .gd file (added to .claude/settings.json by PRC-01) has NOT been observed firing. Its logic was verified by feeding it real stdin JSON in Claude Code's documented shape and it behaves correctly both clean and broken, but no hook output appeared on any live Edit or Write during the session that added it -- most likely because settings.json is read once at session start, before the edit landed. Confirm in a fresh session by deliberately breaking a .gd file with Edit and checking the hook speaks up. If it does not fire, the gate's 'gdscript parses' check is the only line of defence and the hook should be removed rather than left as a comfort that does nothing.
- `med` LF-119 nightly.yml (PRC-08) runs tier 4 unsharded — wire tools/test_parity.py --shard I/N once PRC-05 lands, as a matrix job
- `med` LF-120 nightly.yml (PRC-08) reap step is report-only (no --kill) — switch to a lease-scoped tools/reap.py --kill --lease once PRC-07 lands
- `med` LF-121 install the git hooks tools/install_hooks.py writes — run '.venv/bin/python tools/install_hooks.py' once no other agent has an in-flight commit/push
- `med` LF-122 tools/check.py's three rendered checks (game renders, menu renders, accessibility) should pass an explicit --camera now that CAM-01/CAM-03 landed, so their coverage/contrast numbers stay comparable run to run instead of depending on default camera framing, which CAM-01 changed. Re-baseline after pinning. tools/check.py is owned by another workstream so this was not done as part of CAM-01/CAM-03.
- `med` LF-123 scripts/test/parity.gd's _shed() reaches into AnchorSim.placed directly (and, before PLC-01, free_slots — that array no longer exists) rather than going through the class's own methods. One instance of this was a real bug, found by the first WAR-05 parity run: _shed() wrote s.placed[worst]['online'] = false instead of calling s.set_online(), which bypassed the per-effect list rebuild and left capacity() reading a stale restorer set -- 7 differences across anchor-23/burst at all three difficulties. Fixed for .online; the rest of the function still indexes s.placed and s.free_slots directly, so the next field that needs a hook on write reintroduces the same class of bug silently. Worth a convention note, or making the mutation points the only public way in.
- `med` LF-125 Every real render still overwrites the tracked assets/renders/_calibration.png, so even a single-asset re-render dirties a file that has nothing to do with that asset. Small residual against PRC-13's 'only what changed moves' story; pre-existing calibrate() behaviour. Either write the calibration probe to a scratch path, or stop tracking it.
- `med` LF-126 Anchor-17's default camera framing (8.5,7.5,1.0) puts 2 of the anchor's 12 build slots (index 2 and 3 in anchor[slots] order) off-screen or behind the INCOMING panel — discovered while placing towers via --build for LF-117 verification. Not a bug (players use camera pan/zoom), but worth a note in shot.py's docs so the next session doesn't re-derive which slots are visible by trial and error.
- `med` LF-127 No CLI hook to force a specific pooled combat FX (bolt/arc/flak/mortar) to fire on a chosen frame — verifying LF-117's brownout dim required ~40 blind shot.py sweeps to catch a live projectile on screen, and flak/mortar were never caught firing at all despite the fix applying identically to all four classes. A --force-shot <slot> hook (or a shot_fired debug dump like ABILITY-LIVE's) would make this a two-command job.
- `med` LF-129 Capped-core policies structurally exclude support towers from the closed candidate set, which confounds every escort-value claim on an anchor carrying air units. Concrete case, measured: lance-core (2 ion-lance + pulse-turret fill) LOSES anchor-13 at all three difficulties -- 13 leaks, 0 lives by wave 8 -- while burst (open, unlimited lance) wins all three. The cause is not the escort: neither ion-lance nor pulse-turret has reveal, so a closed policy cannot target reach-skiff or warden-mote at all, a cost burst never pays because scan-relay stays available to it. So the anchor-13 escort claim reads as refuted when what was actually tested was 'no reveal at all'. Needs a policy shape that keeps one designated support tower available outside the core/fill accounting, or the escort question stays unfalsifiable for exactly the anchors it matters on.
- `med` LF-130 mortar-core (2 mortar-emplacement + flak-array fill) is a strictly better board than anything the original twelve policies ever found, and BAL-04 needs to know before it re-tunes anything. It wins anchor-13 at all three difficulties where anti-armour loses hard and brutal, and contributes 29 new distinct winning compositions across 12 anchors -- flak+mortar on 09, 12, 13, 16, 17, 19, 20, 21, 23, 24, plus lance+turret on 14 and 15. Every published pass/fail cell is unchanged, so nothing regressed; what changed is that several Act II and III anchors now have an intended solution the grader had never been able to express. Re-read those anchors' design intent against it.
- `med` LF-133 CLAUDE_CODE_SESSION_ID is per top-level CLI session, NOT per subagent, so the reaper's new own-session lease class is coarser than 'this agent'. Discovered while building the leases: reap.py --json showed a live test_parity.py and a batch of godot --check-only processes carrying the SAME session id as the agent inspecting them, because a sibling subagent fanned out by the same orchestrator inherits it -- which is exactly the fan-out pattern CLAUDE.md prescribes. Consequence: plain reap.py --kill can still silently end a live sibling subagent's process. It is narrower than the old kill-by-command-shape behaviour but it is not safe, and the remaining hole is precisely the multi-agent case the leases were built for. No finer identifier is exposed in the environment; tightening own-session to never auto-kill would break the leftover-cleanup path SessionEnd depends on. Needs either a harness-exposed per-subagent id or a design change -- until then the honest rule is that only --all is ever safe to reason about, and plain --kill is safe only in a single-agent session.
- `med` LF-135 MAX_CONCURRENT_CAPTURES=2 in tools/lease.py is unit-tested but never live-validated. The bound comes from a real measurement -- one Godot capture takes about 8 of this machine's 16 cores, and three concurrent captures drove load average to 18.6 and turned a nine-second frame into minutes (LF-116) -- but the cap itself was only exercised against three threads and a fake workload, not against real concurrent shot.py and gate captures, because doing that safely needs a window when no other agent is working in the tree. Validate it then, and record whether 2 is actually the right number or whether the honest answer is 1.
- `med` LF-138 The HUD threat panel and the dialog both still say 'the lane', singular, now that an anchor can carry up to five. Deferred deliberately by WAR-01 rather than missed. Control reads the bus figure aloud in every brief and tools/say_capacity.py reconciles that, so there is precedent for prose that has to track data -- but lane count is harder, because 'hold the lane' is a line of characterisation, not a number to substitute. Needs a narrative pass over the affected briefs once more than one anchor is genuinely multi-lane, not a find-and-replace.
- `med` LF-139 `--press-at` in main.gd dispatches straight to AnchorView._action_input(), bypassing the real input pipeline — an lf_* action handled in a different node's _unhandled_input (e.g. hud.gd's lf_hud_toggle) is invisible to it. Verifying such actions needs a per-feature CLI hook (see hud.gd's --hud-hidden) rather than --press-at.
- `med` LF-140 Disabled SELL/UPGRADE button text (C_DIM on C_PANEL, 'SELL —' / 'UPGRADE —') measures 4.6-4.7:1 — passes AA but fails the AAA/7:1 bar the rest of the type ladder was solved for (decisions 045/046). a11y.py flags it as a warning on every HUD capture.
- `med` LF-143 Multi-lane camera framing: on anchor-09 the flank lane's exit sits outside the default-zoom frame, because _default_target() can pan but not zoom to fit, and two exits on opposite edges of a 16x13 board cannot both be in frame at one zoom. Presentation only and pre-existing in shape -- decision 056 accepted detail loss at zoom-out, and CAM-01 derives a minimum zoom that fits the board, so the material is all there. What is missing is that the opening framing does not USE it: a multi-lane anchor should open at whatever zoom shows every spawn and every exit, not at 1.0 centred on lane 0. Becomes more visible with every lane added.
- `med` LF-144 sfx determinism failed once for a subagent mid-session and passed on every other run, including twice immediately afterwards on the same tree ('ui_confirm byte-identical'). The agent attributed it to pre-existing audio synthesis nondeterminism; nothing supports that and the check has been green every other time it has run today. More likely a transient under heavy concurrent load, or a partially-written file read by a check racing whatever produced it. Filed so the next occurrence is a second data point rather than a first one -- if it recurs, capture both WAVs and diff them rather than trusting either explanation.
- `med` LF-146 anchor-09's main lane (lane 0) alone, with the original 10-slot board and no flank pressure at all, wins brutal for only 6 of 16 grader policies, several with a 1-2 life margin (control, reserved-mass, mortar-core). This pre-existing tightness is what made LF-136's flank fix so sensitive: any slot/fund diversion for flank defense wiped out those policies' entire margin. Worth a dedicated brutal-tightness pass on anchor-09's main wave table independent of the flank lane.
- `med` LF-147 tools/sweep.py can scale capacity/funds/lives and a uniform per-wave spawn-count weight, but has no way to sweep per-lane wave weight or slot placement. LF-136 (anchor-09's flank fix) needed both -- flank-only spawn tuning and hand-picked exclusive slot geometry -- and had to be done with a throwaway ad-hoc script instead. A --lane-weight knob (scale only spawns tagged with a given lane index) would cover the first half for any future multi-lane anchor; slot placement is deliberately out of sweep's scope per its own docstring and should stay manual.
- `med` LF-150 No profiling hook exists for HUD-layer Control._draw() cost. main.gd --profile (CAM-06) instruments AnchorView, GlowLayer, FxAdditive and CombatFx only, so CAM-04's minimap could not be measured against its own 0.3ms acceptance criterion, and no future HUD performance work can be measured either. The panels are the most redrawn thing on screen after the board. Same shape as the existing hook; it just needs to reach CanvasLayer children.
- `med` LF-153 tools/shot.py can only recognize a successful capture on main.tscn (looks for a literal 'SHOT ' line). draft.gd (and menu.gd, per LF-109) print their own distinctly-prefixed success lines (DRAFTSHOT/MENUFRAME) precisely so they never collide with main.gd's, so shot.py always reports 'Godot never reached the shot' for a --draft or --shot-menu run even when the capture and PNG write genuinely succeeded (confirmed while verifying LF-065/LF-070: DRAFTSHOT ... err=0 printed, PNG written and visually correct, shot.py still exit 1). Generalizes LF-109 to draft.tscn. Fix is a scene-aware success check in shot.py (accept DRAFTSHOT/MENUFRAME as success markers, not just SHOT), or documenting that a --draft/--shot-menu run's real success signal is in stderr's relayed lines, not the exit code.
- `med` LF-155 LF-046 fixed (anchor_view.gd _draw_unit(): three distinct per-faction placeholder colours mirroring combat_fx.gd's FACTION_SHARD, a loud C_FACTION_UNKNOWN fallback for an unrecognised faction, and radius scaled against the real enemy roster's max hp via a cached _enemy_hp_ceiling() instead of a hardcoded 220). Still true as noted originally: this path is only reachable when a sprite is missing, which the sprite coverage gate prevents for tracked content today, so it stays exercised only via the new --dump-placeholder verification hook (main.gd) rather than by any real capture. Worth revisiting if/when free-form or procedurally-added enemy kinds make a genuinely missing sprite reachable again.
- `med` LF-156 Backlog items filed from an agent worktree go stale against a merge, and three did so in one session. LF-068 (no _fire() call sites for the new mechanics), LF-074 (Loadout never called) and LF-069 (leak dialog unwired) all described a HEAD that the merge had already fixed -- each was confirmed by grep at filing time, in a worktree that predated the merge. The cost is not large but it is repeated: an agent is dispatched to fix something already fixed, and the real value it delivers is verification rather than the work asked for. Worth a convention: an item filed from a worktree records the commit it was observed at, and the session-start skill re-checks any item whose observation commit is behind HEAD.
- `med` LF-160 ART-06 shipped two of three emplacement readings and the third is blocked on WAR-10. Idle reads as no arc and no recoil, firing as a cycling reload arc plus recoil, and offline as the existing grey tint with the arc suppressed. SUPPRESSED cannot be distinguished from idle because WAR-10 has not landed: no emp_until or equivalent field exists on the placed record in either engine, so there is nothing for the view to read. This blocks ART-06's own acceptance criterion of three states with three readings. Once WAR-10 adds the field the view side is small -- a separate colour or state on the reload arc, and refusing traverse while suppressed. Hand this to whoever picks up WAR-10.
- `med` LF-164 PRESSURE_FLOOR is deleted (decision 067) and the property it claimed to test still has no instrument. 'The player is pressed against reactor capacity' is the one genuinely good idea the whole game rests on, and peak load ratio cannot measure it: peak is at or above 100 percent on every anchor at every difficulty, which says a greedy policy exists, not that a good board is squeezed. Candidate metrics worth measuring before picking one: the fraction of a WINNING run's sim time spent within some band of capacity; the margin by which the best build clears; or the gap between the best build and the second-best, since a level where one board wins comfortably and the rest fail is a puzzle rather than a decision. Whatever is chosen must be proved to DISCRIMINATE across the 24 anchors before it is trusted -- that is the failure the original made twice.
- `med` LF-168 tools/audio/loudness.py's band is anchored to the bank's own spread and structurally cannot catch a UNIFORM bank-wide drift — every cue N dB hot together moves the mean, the stdev and the band with it, so the check stays green. Stated in the module docstring as a limit of the method rather than a bug, and worth recording separately because it is exactly the failure CLAUDE.md's fifth non-negotiable describes ('a synthesis change that is perfectly deterministic but drifts the whole bank's perceived loudness'). MEASURED consequence of the same property at the per-file level: the SFX band is 23.2 LU wide because cues are deliberately NOT matched to each other (a hover tick and a heavy footfall should not be equally loud), so ui_confirm.wav at +6 dB PASSES — proved, not assumed. Loud-edge and quiet-edge cues do fail at +/-6 dB (shield_break -8.89, threshold_idle -36.77), so the check is real, just blunt in the middle of the band. Fix needs a SECOND signal, not a tighter band: track the bank's mean LUFS against a frozen historical baseline committed alongside the bank, so a uniform shift moves the mean away from a number that does not move with it. Do NOT respond by narrowing the per-file band — that would fail cues the sound design intends to be quiet, and decision 011 already records what happens when a metric gets tuned against itself.
- `med` LF-176 OWNER PLAY FEEDBACK 2026-07-31: most turrets are barely able to reach the enemies on the field, so the board feels slow and a lot of built emplacements contribute little. This is the owner playing, not a measurement, and it should be treated as the primary signal — the grader has never had a way to see it. Why the current tooling is blind to it: the sim only asks whether a build WINS, and tools/density.py reports units in flight, so a board where every gun fires at the edge of its range for a fraction of each pass grades identically to one where guns are sited well. Nothing measures per-emplacement uptime, time-on-target, or the fraction of the lane a slot actually covers. Concrete first instrument, cheap and pure-Python: report per-emplacement 'fraction of ticks with a live target in range' and 'share of total damage dealt' out of sim/engine.py, then look at the distribution per anchor — the owner's complaint predicts a long tail of near-zero contributors. Likely causes to separate once measured: (a) slot geometry, the slots authored into each anchor sit too far from the lane, which is what PLC-01 free placement and PLC-04's candidate lattice exist to fix; (b) range values in data/towers.json are simply low relative to lane length; (c) the autobuild/grader policy fills slots in priority order regardless of coverage (LF-053, LF-072), so what the owner sees when a level plays itself is not a sited board at all. Do not tune range numbers before the instrument exists — that is exactly the fitting-to-today's-data move decision 067 rejected.
- `med` LF-182 tools/parity_costs.json's LPT bin-packing is tuned on local 16-core timings and redistributes unevenly on hosted runners. MEASURED on the first real CI firing of parity-shard.yml (PR #104, run 30673542813): shard 0/4 7m47s, 1/4 9m05s, 2/4 5m37s, 3/4 7m42s — a 1.6x spread, all four passing. Not a problem at this scale, since the whole 1440-run matrix still finishes in ~9m13s wall clock with the shards in parallel, and the pre-merge projection of 'around 8 minutes per shard' held honestly. But the packing is doing less work than it thinks: the cost table is populated from runs on a 16-core box and hosted runners have different relative per-anchor costs, so the LPT assignment is optimising against the wrong weights. This is the first thing to look at if the matrix ever needs to be faster — better than adding shards blindly, which just multiplies the fixed per-job setup. PRC-19 (#85, right-size the parity matrix from measured discrimination) should own it, and should use hosted timings rather than local ones to rebuild the table. Note also that 288 of the 1440 runs exercise one shared dispatch mechanism across all 24 anchors, which #85 already records — cutting those is a bigger win than rebalancing what remains.
- `med` LF-183 AnchorView._draw_editor_overlay() (the in-editor path/IN/OUT preview) does not apply Iso.height_offset() to its waypoint polyline or markers, unlike _draw_board()/drawables()/_draw_range() which TER-01 made elevation-aware. Cosmetic only — editor authoring aid, never seen in a real playthrough — but a path crossing a raised region will preview at ground level until this is fixed.
- `med` LF-190 LF-172 IS ANSWERED and the answer is that BAL-01's headline was WRONG WHEN PUBLISHED, not that it drifted. Two independent lines of evidence, both measured. FIRST, git (LF-188): between BAL-01 (4daeb7c) and the re-grade that disconfirmed it (00c64ca), data/anchors/ data/towers.json data/enemies.json data/tuning.json sim/engine.py and scripts/anchor_sim.gd ALL have zero changes, the only commits touching data/ sim/ or scripts/ are a PRESSURE_FLOOR deletion (which feeds the anchor verdict, not per-cell win/loss, and was measured dead at 0 of 72) and a whitespace-only reindent of a view script, and the 20-policy roster is byte-identical. SECOND, the method hypothesis is now tested and also dead. BAL-01 claimed surge flips 9 of 72 cells from LOSS to WIN, so nine cells must have had NO winner without surge. Every plausible baseline reconstruction was graded — 11 policies (base minus greedy-overdraw), 12 (base only), 15, 16 (base plus capped-core, which is what BAL-01's own report claims it used) and 19 (everything except surge-on-peak) — and ALL FIVE produce ZERO flips. Run twice: once at today's post-LF-181 ranges and once at the PRE-LF-181 ranges via dataclasses.replace, to eliminate the range change as a confound. Byte-identical conclusions both times. The decisive number is that ZERO of the 72 cells are unwinnable by any policy at ANY baseline including the 11-policy one, so 'loss without surge' has nothing to be true of. surge-on-peak is the sole winner on 0 of 72, reproducing 00c64ca exactly. CONSEQUENCE: the chronicle correction already published stands and needs no further amendment, LF-172's 'why it moved is unknown' can be retired, and the four candidate causes it named (LF-098 LF-129 LF-130 LF-136) are all ruled out. The three unrecorded cells of the nine remain unrecoverable but no longer matter, because the claim does not reproduce under any reading of what its baseline was. WHAT REMAINS WORTH KEEPING from LF-172 is the process finding, not the balance one: a measurement was published with three of its nine data points never written down anywhere, which is why it could not be audited and had to be refuted by reconstruction instead
- `med` LF-191 TRAP for anyone debugging a Windows CI hang: PowerShell's Process object CPU / TotalProcessorTime is NOT a trustworthy 'is it stalled' signal for Godot on windows-latest. MEASURED during BAL-06's hosted Windows parity work: a CPU poll showed cpu_seconds=0 and working_set_mb=6.5 across all 12 samples over 4 minutes, on a run that PROVABLY completed real and correct work — anchor-05 alone finished at 55-60s emitting fully valid PARITY_JSON for all 48 of its combinations while the poll read a flat zero throughout. The flatline is a measurement artifact of how the Process object reports for this child, not evidence of a stall. Consequence: it sent the diagnosis down a false path, because 'no output plus no CPU' reads as wedged when the real state was 'working, slowly'. Distinguish 'no output' from 'no CPU' using wall clock against a known-good baseline — the single-anchor smoke path in parity-windows.yml completes in 20-30s and is the right reference — rather than CPU polling alone
- `med` LF-192 The Windows parity leg's per-anchor cost is both HIGHER than the local 16-core benchmark and RISES STEEPLY across anchors, which is why an unsharded full run cannot fit a sane job budget on windows-latest. MEASURED during BAL-06: sweeping anchors individually on the hosted runner, anchor-01 through anchor-04 completed cleanly with a rising trend of 24.1s to 48.1s, and anchor-05 alone took 55-60s. Extrapolating the observed 562s for 360 runs to the full 1152 gives roughly 1800s, which is almost exactly where the original unsharded 29-minute run died against test_parity.py's own 1700s no-output timeout. So the first Windows failure was never a hang — it was real computation that ran out of clock, and it looked exactly like decision 071's precedented class_name/import-cache hang without being it. The import step was added on that hypothesis and MEASURED NOT TO HELP: a post-import CPU poll was identical to the pre-import one. Sharding 4-way with PRC-05's existing --shard I/N mechanism is what actually fixed it. Note also that tools/parity_costs.json's LPT table is built from LOCAL 16-core timings, so it is the wrong weighting for the Windows shards just as LF-182 already records for the hosted Linux ones; only shard 0/4 has a real Windows number so far (562s for 360 runs)
- `med` LF-196 OWNER-FACING CONTROLS AUDIT: LF-177 (game speed-up) was filed as 'deferred to a later iteration' and is in fact SHIPPED — the owner did not know, and asked about pacing again on 2026-08-01. data/tuning.json authors speeds [1.0, 2.0, 3.0], anchor_view.gd cycle_speed() wraps through them on the lf_speed_cycle action, project.godot binds that to physical keycode 96 (the backtick key) and gamepad button 7, and hud.gd renders the current value as 'Nx SPEED' top-left. It is implemented exactly the safe way LF-177 itself demanded: _accum += delta * speed runs the SAME fixed tick more times per frame rather than scaling dt, so parity is unaffected — which is why 1440 runs still pass. THE FINDING IS NOT ABOUT SPEED, IT IS ABOUT DISCOVERABILITY. A shipped player-facing control the owner does not know exists is worth nothing, and the backlog said 'deferred' for long enough that it got quoted back as outstanding. Two things worth doing. (1) Audit every player-facing control against what the owner has actually been told — speed is unlikely to be the only one; candidates are the minimap focus key, edge scroll, the pause menu volume controls, sell and upgrade, and gamepad build. (2) Treat 'shipped but unannounced' as a distinct backlog state from 'open', because closing it silently is what let this happen. Related to LF-195 (stale items cost a dispatch) but worse: this one cost the OWNER a play session at 1x speed
- `med` LF-202 FOOTPRINT_RADIUS is a single shared rules constant (0.45 tiles, defined in both sim/engine.py and scripts/anchor_sim.gd), NOT the per-tower footprint_radius field PLC-01's task list anticipated in data/towers.json. No such field exists yet, and data/towers.json was deliberately out of scope for PLC-02 because decision 074 had just re-tuned every range and a second edit in the same window would have made the two changes inseparable in any measurement. CONSEQUENCE TODAY: every tower gets an identical footprint, so the overlap test and the lane-standoff test treat a pulse-turret and a mortar-emplacement the same. Zero risk right now — VERIFIED, not assumed: no two authored slots in any of the 24 anchors are closer than 0.9 tiles, so the shared 0.45 can never produce a false overlap against the existing content. It becomes real the moment free placement is player-reachable, because a player will put emplacements as close together as the rule allows, and a siege battery occupying the same footprint as a scan relay is a design statement nobody made. THE WORK: author a per-tower footprint_radius in data/towers.json plus towers.schema.json, and read it in place of the constant in both _placement_reason() implementations — keeping the term order identical character-for-character, which is the discipline that made PLC-02's 48,837-position differential come out empty. Belongs with PLC-04 (candidate lattice) or PLC-07 (placement UI ghost), whichever lands first, since both need to draw or reason about real per-tower geometry
- `med` LF-203 board_props.gd's ring and bindstone props (_ring_ground / _bind_ground) still project at elevation level 0 regardless of the height of the path's entrance and exit tiles. Same class of floating-prop bug TER-07 just fixed for the board itself, and explicitly outside TER-07's task list, which named only the ground sigils and the ring ticks. Harmless today because no authored anchor elevates its lane ends — but it becomes visible the moment one does, and it will read as a terrain bug rather than a prop bug, because the ground under the prop will have moved and the prop will not have. The fix is the same shape TER-07 already applied to _build_edge, _build_ground_sigils and _build_ring_ground_ticks: read the tile's own height instead of assuming 0. Worth doing alongside whichever issue first authors an anchor with raised lane ends — TER-09 (the lane climbs, waypoint z) is the obvious one, since it is the issue that makes raised lane ends exist at all
- `med` LF-205 CORRECTION to LF-196, which overstated its own finding — the controls ARE surfaced in the HUD, and the failure was narrower and worse than "nobody told the owner".

LF-196 claimed the game speed control and the three abilities were undocumented anywhere the owner would see them. A full-frame capture of anchor-01 disproves that directly. The HUD renders, bottom-left, a BINDSTONE panel listing "[1] SURGE - CHARGE 0%", "[2] OVERCHARGE - READY", "[3] SHUTTER - READY" — each with its key, and with live charge state. And top-left, under the wave line, it renders "1x SPEED" with the key in parentheses. Both controls are on screen, keyed, in the default view, with no interaction required.

SO THE REAL FAILURE IS NOT DISCOVERABILITY IN THE GAME. It is that the BACKLOG said LF-177 was "deferred to a later iteration by the owner's own framing", that text was read and quoted back as though it described the present, and nobody — not the agent, not the coordinator — opened the game to check before repeating it. The owner then asked about pacing again and was told a shipped feature was pending.

That makes this the same family as LF-195 (two items fixed while still reading as open) and LF-156 (items filed from a worktree going stale against a merge), but with a sharper edge: those cost a wasted dispatch, this cost the owner a play session AND produced a confident wrong statement to them. A backlog is a record of what someone believed on the day they wrote it, and this project already knows that about DECISIONS.md and about the chronicle — the backlog has no equivalent convention.

WHAT TO ACTUALLY DO, replacing LF-196's proposed "audit every player-facing control against what the owner has been told":
(1) Verify against the RUNNING GAME, not against the backlog, before telling the owner anything is missing. A capture is nine seconds.
(2) An item that describes a deferral should record the commit or date it was true at, exactly as LF-156 proposed for worktree-filed items and which is still not implemented. Three separate items now point at that same missing mechanism.
(3) The narrow real gap that remains: there is no single place a player can see ALL the controls. The HUD surfaces speed and abilities; sell, upgrade, power-toggle, call-wave, target-cycle, minimap-focus and HUD-hide are keyed and NOT shown anywhere on screen. That is a genuine and much smaller finding than LF-196 claimed, and it is the one worth building — a controls overlay, probably on the pause menu, which already exists and already has volume controls.

Keep LF-196 open only for item (3). Its first two thirds are retracted.
- `med` LF-206 OWNER INTENT 2026-08-01: package Latticefall for a storefront so other people can install and play it. Verbatim: "I anticipate that at some point this game is actually going to be built up and put on the Steam marketplace or another marketplace that I could push it to for other individuals to pull it down and play." This is a spec note and a scouting task, not work to start now — the game is playable but only from the Godot editor, and several things below have to be true before a build is worth shipping to a stranger.

WHAT DOES NOT EXIST YET, and each is a real prerequisite rather than a nicety.

(1) NO EXPORT PIPELINE AT ALL. There are no export presets in the repository, no export_presets.cfg, and nothing in tools/ produces a binary. Godot needs export templates matching the exact engine version, and the project targets 4.7 with the GL Compatibility renderer. First task is a headless export producing a Windows executable that launches on a machine with no Godot installed and no repository checkout — and that must be verified on a clean machine or a container, because "it runs on the dev box" proves nothing here; the .godot import cache and the LFS working copy are both present there and neither ships.

(2) THE IMPORT-CACHE ASSUMPTION INVERTS. Everything about this project's local workflow assumes the editor imports assets into .godot/ and that the cache is build output, gitignored, and rebuilt on demand. An exported build has that cache baked into the .pck instead. So the export is the FIRST context in which a missing or stale import is unfixable by the player, and the class_name/global-class-cache trap that makes a scene hang rather than error becomes a shipped hang. Export verification therefore has to include actually playing an anchor from the packaged build, not just confirming the binary starts.

(3) NO LICENCE, AND THE AUDIO POSITION NEEDS RE-READING BEFORE MONEY IS INVOLVED. The repository has no LICENSE file. Decision 038 records that sound effects are synthesised in-repo (fine) and that music is generated under the owner's own subscription — that arrangement needs checking against the generator's terms for COMMERCIAL distribution specifically, which is a different question from personal use and is the owner's call, not an engineering one. Fonts are vendored under addons/ and LFS; their licences need auditing the same way. This is the item most likely to be discovered late and to block a release date.

(4) SAVE COMPATIBILITY BECOMES PERMANENT. progress.json is currently free to change shape because the only player is the owner and the only save is on this machine. The moment a stranger has a save, a schema change is a migration. Decide before release whether saves are versioned and migrated or explicitly disposable, and write it down; tools/save_roundtrip.py already proves round-trip integrity and is the natural place to also prove a migration.

(5) NO CRASH OR TELEMETRY PATH, and no way for a player to report anything. Worth deciding deliberately rather than by omission — this project's whole method is that claims are falsifiable, and shipping to strangers with no signal coming back is the opposite of that.

STOREFRONT SPECIFICS TO SCOUT, not to decide yet. Steam has a one-time app fee per title, requires a bank and tax setup that takes real calendar time to clear, wants store assets at fixed pixel sizes, and expects a build uploaded through steamcmd or the Steamworks SDK — none of which is hard, all of which is lead time. itch.io is the cheap and fast alternative and is the sensible place to put the first public build regardless, because it costs nothing, needs no approval, and exercises the same export pipeline. Recommend scouting itch.io FIRST as the forcing function for (1) and (2), and treating Steam as the second step once a packaged build has survived contact with someone who is not the author.

SEQUENCING NOTE. Do not start this before the Theatre Scale programme's E3 placement work is playable — the board size target moved to 48 squared (decision 073), hand-authoring is dead, and the layout generator (LF-080) is on the critical path. A storefront build of the current 18x15 content would be packaging a game the programme is actively replacing.
- `med` LF-207 PLC-06 needed a build-candidate ordering for the autobuild fallback and PLC-04 does not exist yet, so it carries a minimal deterministic 1-tile fallback scan in scripts/anchor_view.gd instead. That scan is correct and deterministic but it is NOT the candidate lattice PLC-04 is meant to provide, and PLC-06's own task list says to reuse PLC-04's ordering. Revisit when PLC-04 lands: the fallback in anchor_view.gd should be deleted and replaced by whatever PLC-04 exposes, so there is one candidate ordering in the project rather than two that can drift. Low urgency — the fallback only runs for --autoplay and the grading harness never uses it, since the grader sources candidates from _slot_priority() in the rules engines. But two orderings that disagree would be exactly the kind of thing that shows up later as an unexplained difference between what the autobuild does and what the grader says is possible
- `med` LF-208 The lane magnet, as PLC-06's spec described it, ACTIVELY FIGHTS THE PLAYER and was removed after being built and played. It needs a different formulation before anyone attempts it again. THE MECHANISM, measured rather than reasoned about: the free cursor moves along four tile-space diagonals that project to screen up/down/left/right, and each of those four is exactly 45 degrees from any axis-aligned lane tangent. The magnet scored alignment as a dot product against the raw tile-space lane tangent, so on an anchor whose lanes are axis-aligned — which is the common case, and anchor-01 specifically — EVERY direction the player pressed scored as "aligned enough" and the magnet pulled the cursor back toward the lane it was trying to leave. MEASURED: three presses of screen-right from a lane-adjacent start should land at (8.06, 3.94), which is legal; with the magnet enabled it reached (7.57, 3.61), which still returns REASON_LANE_STANDOFF. The player presses away from the lane three times and remains unable to build. A helper that fights the player is worse than no helper, so it was cut rather than tuned — tuning the strength would only have changed how far the fight went. IF IT IS ATTEMPTED AGAIN, the fix is to key the assist off the SCREEN direction the player actually pressed rather than off a raw tile-tangent dot product, so that "I am moving away from the lane" is expressible at all. Note also that snap-on-build already covers most of what the magnet was for: lf_build ring-searches within SNAP_RADIUS 0.6 tiles and lands on the nearest legal position, so a player who aims approximately still gets a clean placement. The magnet was solving a problem that the snap mostly solves already, at the cost of making movement feel wrong, and that trade should be re-justified before the idea comes back
- `med` LF-210 autoloop: if the loop is SIGKILLed, the detached session survives and bills tokens with nobody watching. Its pid is now printed and written to .cache/autoloop-status.json so it can be ended by hand, but reap.py does not know about 'claude' sessions and --status does not check whether that pid is still alive. Teach the reaper about a leased autoloop child.
- `med` LF-216 PLC-07 FOLLOW-UP: remove the tile_slot render asset, which is now drawn by nothing. PLC-07 retired tile_slot from the board -- free placement means legality is a predicate over a continuous position, so there is no set of allowed tiles to paint -- and DECIDED against the alternative the spec named, repurposing it as the buildable-area wash. The reason is the sprite itself: it is a raised bevelled pad with a teal ring and four corner bolts, discrete geometry with real height, and tiled across a region it reads as a field of hardpoints, which reinstates the exact "these are the slots" language PLC-07 exists to remove. A wash has to be a tint, not an object, and it is now a tint over the ground. WHAT IS LEFT is purely mechanical and was deliberately NOT bundled into PLC-07's pull request, because it triggers the art pipeline and a re-import and that is the step which blanks the owner's running game if mishandled (LF-075) -- tangling it with UI work would have made a red sprite atlas check look like a UI regression. THE STEPS, in the order CLAUDE.md fixes: drop tile_slot from render.py's ASSETS dict, drop it from gen_assets.py's PROP_ALLOWLIST (it is a prop, not a data id, which is why asset coverage rather than sprite coverage governs it -- PLC-07's own spec had this backwards and said sprite coverage would fall to 21 of 26; it would not, that check compares enemy and tower ids only), delete the eight PNGs (4 yaws x albedo/glow), re-run pack_atlas.py, re-import, and confirm the sprite atlas check goes green on the new page hash. EXPECT the atlas cell count to fall from 568. ALSO NOTE the C_SLOT constant in anchor_view.gd is now referenced only by the no-atlas fallback path in _draw_board and can go at the same time. Say so before re-importing -- the owner plays out of this same tree
- `med` LF-218 Act 1 and act 2 anchors attrit the wave in the first fifth of the lane; act 3 does not, and that difference IS the residual near-zero tail. MEASURED in live unit-ticks per 5 pct of lane, summed over 20 policies x 3 difficulties: anchor-01 reaches 50 pct cumulative presence by bucket 3 (15 pct of lane) and anchor-13 by bucket 4; anchor-20 not until bucket 9 (45 pct), with 11 pct of presence still in the last quarter. Near-zero emplacement rates track it exactly - act 1 16 pct, act 2 11 pct, act 3 5 pct, after the LF-185 re-siting pass. CONSEQUENCE: slot re-siting can only move guns INTO the live zone, it cannot lengthen the live zone, so the tail migrates rather than vanishing (post-LF-185, anchor-11 (2,8) is 45/51 near-zero and anchor-13 (10,4) is 34/57 - slots that were fine before). Chasing near-zero to zero converges on every slot stacked at the lane entrance, which is fitting to the metric and is exactly what decision 067 rejected. THE LEVER for the rest is enemy hp/speed/spawn pressure in the act 1-2 wave tables, not slot geometry. This is BAL-04 territory and it WILL move the grades, so it must be graded as a balance pass rather than bolted onto a siting change. Note LF-187 already says decision 074's range numbers must be re-derived at 48x48 anyway
- `med` LF-220 anchor-02 brutal lost two distinct winning builds (6 -> 4) in the LF-185 re-siting pass. Recorded so it is not later discovered as an unexplained narrowing. Still clean - sim/run.py reports ok=True, death wave unchanged at 2, and four distinct winning builds is well above the single-solution floor - and it is the ONLY cell of 18 (six anchors x three difficulties) that narrowed; distinct viable builds went UP on anchor-10, 11, 12, 13 and on anchor-08 standard. The likely mechanism is that concentrating slots into the high-presence early stretch of the lane makes a few strong sitings dominate, which removes weaker but previously-viable build orders. If anchor-02 brutal narrows further in any subsequent pass, this is the entry to start from. Before/after grade JSON was produced in the LF-185 pass
- `med` LF-221 sim/coverage.py --selftest was RED on main and nothing noticed, because NO GATE TIER RUNS IT. Found during the LF-217 pass and fixed there, but the gap is the real item. MECHANISM: the selftest's far-slot probe built at (500,500) to prove a slot with zero coverage measures zero; PLC-02's _placement_reason() then began answering out_of_bounds for that position, so the scheduled build stopped landing and the selftest failed with 'the scheduled build never landed' - a message about the harness, not about coverage. Proved against git show HEAD:sim/coverage.py run in isolation: 'HEAD placed slots: []'. Fixed in LF-217 by widening the in-memory probe anchor to 40x40 and moving the probe to (30,30); data/ untouched. WHY IT MATTERS: this selftest is the ONLY runtime proof that InstrumentedSim cannot move an Outcome - it runs the same (anchor, policy, difficulty) through both Sim and InstrumentedSim and asserts the Outcomes are field-for-field identical. Without it in the gate, a rules change in an unrelated workstream can silently invalidate every coverage measurement the project takes, and did, for at least two merges with nothing red anywhere. THE WORK: put it in a tier. It is ~2m43s in full, which is too expensive for tier 2 (already over its 28000ms budget, LF-178) - so either tier 3/4, or add a cheap variant that runs only the outcome-parity leg on a single anchor and put THAT in tier 2. Prefer the cheap variant plus the full run at tier 4
- `med` LF-222 PLC-04's candidate lattice ENLARGES LF-053/LF-095 rather than solving it, and PLC-04 must not be read as a fix for them. Every Policy is a total preference order and _try_build fills greedily from the top, so a graded build is all-of-one-thing. With 12 authored slots that produced 'twelve arc nodes'; with the ~366-763 legal lattice candidates per anchor at half-tile pitch it produces 'as many as _effective_cap() and the money allow'. MEASURED during the PLC-04 pass. The count has not actually moved yet because _effective_cap() still falls back to len(slots) - the day that fallback goes, it will. BAL-02's capped-core policies are the fix. Recorded here because PLC-04's own spec asks for the note and because a bigger candidate set makes a monoculture build look more deliberate than it is
- `med` LF-225 say_capacity.py and the dialog capacity gate check only track the spoken '<N> megawatts' phrase, so a brief that refers back to its own capacity a SECOND time as a bare word-number goes stale silently and the gate stays green. FOUND applying LF-223: anchor-04's capacity went 96 -> 94, say_capacity.py rewrote the spoken figure, and the brief still read 'Ninety-four megawatts. Shield wall's signed off - forty of those ninety-six if you raise it.' The tool structurally cannot reach it - its regex is ([A-Za-z][A-Za-z \-]*?)\s+megawatts, so a bare number with no unit after it is invisible. Fixed by hand in the LF-223 pull request; the GAP is the item. THE FIX: after rewriting the megawatt phrase, also scan the brief for the OLD spoken figure as a bare word-number and either rewrite it or refuse and report - the tool already knows how to refuse, it does exactly that when a brief contains two capacity-sized figures. Same class as the check that verifies output matches input and cannot see a generator wrong in a stable way
- `med` LF-227 CI's Windows parity leg does not run on a data-only change, but the gate's own parity digest says data IS part of parity's input. MEASURED on PR #143 (LF-223, four anchor JSON files plus one dialog file): .github/workflows/parity-windows.yml triggers on pull_request paths sim/engine.py, scripts/anchor_sim.gd and scripts/test/parity.gd ONLY, so no Windows job ran; meanwhile tools/check.py's digest is described in its own skip message as covering '4 rule file(s) + 77 data file(s) + godot --version' and DID invalidate, forcing a fresh 1440-run Windows leg locally. Two components disagree about what parity depends on, and the more conservative one is the one that does not run in CI. WHY IT MATTERS: a data change alters which code paths the rules take - anchor-11 in that same PR became the first shipped anchor where max_emplacements exceeds len(slots), newly entering a branch no anchor had ever entered - so a data change absolutely can surface a platform divergence that a rules-file diff would not. Decision 078 exists because atan2/sin/cos/pow/log/exp diverge between Windows Godot and CPython on 0.03-4.32% of float64 samples, and THE OWNER PLAYS THE WINDOWS BUILD. THE WORK: add data/**/*.json (or at minimum data/anchors/**, data/towers.json, data/enemies.json) to parity-windows.yml's pull_request paths, and check parity-shard.yml for the same gap. COST NOTE: the header comment says weekly rather than daily was chosen deliberately on cost, so widening the PR trigger needs that budget re-checked rather than assumed
- `med` LF-230 sim/coverage.py's InstrumentedSim overhead scales with EMPLACEMENT COUNT and will not survive the theatre-scale boards. MEASURED, same policy (greedy-overdraw, standard) on both: anchor-20, 12 emplacements -- plain Sim 0.24s, instrumented 0.51s, 2.1x. Generated 48x48 4-lane board, 78 emplacements -- plain 3.88s, instrumented 26.41s, 6.8x. A full --anchor coverage pass (20 policies x 3 difficulties) is ~2.5 min on anchor-20 and ~21 min on the 48x48 board; a 64x64 board derives 131 emplacements and would be worse again. The cause is that InstrumentedSim's post-tick reconstruction walks placed x units directly, while the rules themselves got WAR-03's uniform spatial grid and did not regress -- so the instrument, not the engine, is what stops scaling. Outcome parity is intact (Outcomes compared field-for-field equal on both anchors in this measurement), so this is purely a cost problem. Worth fixing before BAL-04 needs coverage numbers on generated content, because 24 large anchors at 21 min each is 8 hours. The obvious fix is to reuse the engine's own grid for the in-range test rather than rescanning.
- `med` LF-233 AnchorSim.available_slots() should be renamed unoccupied_authored_slots(). It now has exactly three callers left in the running game - anchor_view.gd::_next_autobuild_candidate(), main.gd::_build_one()'s no-position fallback, and scripts/test/facing.gd - and all three mean 'an authored position nobody has taken yet', which is a debug and verification convenience rather than a rule. LF-228 was the last place it was standing in for a real quantity (the board's capacity) and the mistake it caused was SILENT FOR TWO MILESTONES: _on_built() computed ward engagement as placed + available_slots(), which grows with every build under free placement, so the wards-full dialog cue could never fire in ordinary play. The name is what made that read as correct. Low risk - three call sites, no rules file involved, and anchor_sim.gd's own callers are unaffected because the method is not used by the sim itself. Worth doing while the reason is fresh
- `med` LF-234 LF-229's premise was WRONG about how narrow the masking window is, and the correction matters for act 1. That ticket said all 24 shipped anchors have presence ceilings of 75-95 pct, so verdict()'s (0)-before-(b) short circuit could only mask a low ceiling on a board unlike any that ships. MEASURED at range basis 8189779cd568 while fixing it: anchor-02 68.3 pct, anchor-03 65.3 pct, anchor-04 50.1 pct, anchor-05 41.8 pct - ALL FOUR below BOARD_CEILING_LOW. No verdict label actually moved when the ordering was fixed, but only because those four already carry 19-31 pct near-zero tails and were therefore already reaching the (b) branch by a different route. So the window is narrower than the ticket implied but it is REAL, and what sits under the threshold is act 1's entire opening stretch. WHAT THIS MEANS: anchors 02 through 05 are boards where the best legal positions available reach only 42-68 pct of the live wave - the LEVEL's own slot-and-range budget is the limit, not the player's siting. That is the same conclusion LF-187 reaches from the other direction (a fixed absolute range covers a shrinking fraction as lanes lengthen) and the same four anchors decision 081's re-balance found thinnest. Worth treating as one finding rather than three when BAL-04 re-derives range: act 1's ceilings are low TODAY, at 18x15, before theatre scale makes it worse
- `med` LF-235 AudioDirector.sfx() has no cue log, so no scenario can assert what the game actually PLAYED - every audio assertion in this project is really an assertion about the caller's arithmetic. FOUND while fixing LF-232: proving the ward cue's rung sequence needed a TEMPORARY field added to anchor_view.gd::export_state() and removed again before shipping, because the cue index was visible nowhere outside _on_built(). Both LF-228 and LF-232 were silent for the same reason. LF-174 already lists audio_director.gd as zero-coverage; this is the concrete mechanism. THE WORK: a bounded ring buffer of the last N cue names, exposed through Audio.report() or a view.audio.recent key in export_state(). It makes 'the brownout duck fired', 'the deny tone played and not the build tone' and 'rung 6 played exactly once' assertable from a scenario, none of which can be checked today. Bounded is load-bearing - an unbounded log on a 48x48 board with 78 emplacements and ten waves is a memory leak, and the gate's own bounded-subprocess rule exists for the same class of reason
- `med` LF-237 scenario lf226-fallback is the most expensive non-parity check in the gate at 75.8 s local; next is anchor grades at 63.7 s and scenario smoke at 37.4 s. SCENARIO_TIMEOUT_S is 300 s and its comment already records CI being slow enough to force 120 -> 300 once. THE COST IS STRUCTURAL, not a slow implementation: the 12th and 13th emplacements on anchor-11 are funded by wave-2 and wave-3 bounty, so the run cannot end before roughly frame 2900 even at 3x, the fastest authored speed. Two things to do rather than one. First, RE-MEASURE ON CI before assuming the 300 s margin holds - the local figure is 16-core and CI is not. Second, note that tier 3 is now 355 s locally and has absorbed two 60-80 s checks in one day (anchor grades, this one); tier 3 has no asserted budget the way tiers 1 and 2 do, so nothing will tell us when it stops being a usable PR gate. Consider giving tier 3 a --budget assertion like the lower tiers have, which is the only mechanism this project has that reliably catches a tier quietly becoming too slow to use
- `med` LF-238 AnchorSim.available_slots() (scripts/anchor_sim.gd) TRUNCATES a slot to Vector2i(int(sx), int(sy)), so on a fractional-slot anchor the two verification hooks that consume it site an emplacement at the FLOOR of the authored position. Found auditing LF-219 (schema widened from integer to number, so fractional slots are now legal content). Callers: anchor_view.gd _next_autobuild_candidate() (--autoplay/autobuild) and main.gd:452 (--build <tower>). If the floored position is illegal -- e.g. a slot at (7, 4.6) beside a lane on y=4 floors onto the lane tile -- build_at() returns false and the build SILENTLY does not happen; the autobuild loop exits on its placed_one flag rather than hanging, so nothing is red. Nothing else in GDScript truncates: the rules (build_at/_is_placeable/_occupied/effective_cap) read only slots.size(), the editor overlay at anchor_view.gd:2824 draws through float(slot[0]) and is already correct, and fx_additive.gd hashes PLACED x/y not authored slots. board_props.gd:422 also floors slots into its prop-exclusion tile set, which is cosmetic (a prop may draw over a fractional slot). THE FIX: available_slots() should return the authored floats -- but it is a presentation accessor whose Vector2i shape its callers depend on, so this is a small typed change across three files, not a one-liner. Zero shipped anchors are affected (all 24 still author integers); validate_data.py WARNS the moment one does not, naming this call site
- `med` LF-239 tools/genboard.py still emits INTEGER slot positions now that LF-219 has widened data/schema/anchor.schema.json to number. Not a bug -- deliberately left, and the docstring on place_slots() now says why rather than citing the old schema constraint. What blocks it: render() indexes an ASCII preview grid by slot (cell[y][x]) and would TypeError on a float; the generator binds the COUNT far more than the positions (_effective_cap()'s fallback -- see LF-222); and moving the positions moves every generated board's numbers, including the byte-identical output --selftest asserts. THE WORK, when PLC-04's lattice or LF-080 wants it: site slots on the same half-tile lattice build_candidate_lattice() uses, floor in render() only, and re-baseline --selftest in the same change. Worth doing then because sim/coverage.py reports per-slot presence off exactly these positions, so an integer-only generator understates what a generated board's best sitings are worth
- `med` LF-240 TIER 2 HAS RE-BREACHED ITS BUDGET AND THE FIX IS MEASURED: move 'sim determinism' from tier 2 to tier 3. NOTE LF-178 is correctly marked done - it was closed by the facing harness move and that closing evidence is real. This is a NEW re-breach caused by PLC-04, not a reopen. THE ANALYSIS, from the seven gate JSON artefacts of 2026-08-03: sim determinism is the ONLY single demotion that clears 28,000 ms on all seven runs (worst tier-2-after 25,375 ms, margin +2,625 / 9.4 pct, 0 of 7 still over). Every other candidate fails on at least one run - godot boots leaves 1 of 7 over, sprite atlas 5 of 7, hooks configured 3 of 7, terrain parsers agree 5 of 7, sfx loudness 7 of 7. Harness overhead beyond the sum of per-check ms is under 1 ms on all seven, so a tier<=2 subtotal exactly predicts a standalone tier-2 duration. WHY THIS CHECK AND NOT THE CHEAPEST: it IS the re-breach. LF-178 recorded sim determinism at 4.8 s; today's median is 8,188 ms, and the jump is locatable to one commit - 6,282 ms at 289f65f, 8,214 ms at e1896da (PLC-04's candidate lattice), +31 pct across that boundary. Tier 2 was 27,225/27,790 ms after the facing harness move and is now ~30,500; sim determinism grew ~3,400 ms. That is the whole regression. Its cost curve is also CONTENT-SCALED, which is the wrong curve for a pre-push tier: it runs sim/run.py --anchor anchor-01 twice and run.py grades every policy x all three difficulties, so cost tracks policy count x lattice candidate count, and LF-222 records that _effective_cap() still falls back to len(slots) - when that fallback goes, candidates per anchor go to ~366-763 and this check breaches again on its own. RISK x FREQUENCY: the only change class that can break run-to-run determinism is a change under sim/, and CLAUDE.md already mandates tier 4 locally for anything touching the rules, so at tier 2 this check is paid on every push and fires only on pushes for which tier 2 is not the prescribed gate. NOTHING IS LOST PER-PR: gate.yml's gate-tier3 job runs tier 3 on every pull_request with no path filter, and tier 3 is a superset of tier 2. NOT RECOMMENDED as a substitute: shrinking it with --difficulty standard - plausible, but no measurement of a single-difficulty run exists in any artefact, so adopting it would be acting on an estimate
- `med` LF-241 GIVE TIER 3 A --budget OF 480,000 ms, introduced NON-ASSERTING. LF-237 asked whether tier 3 needs a budget; this is the derived answer rather than a fitted one. THE DERIVATION IS EXTERNAL, not a fit to today's data: the binding constraint on tier 3 is that it must run in ONE FOREGROUND BASH CALL, because CLAUDE.md is explicit that a backgrounded command re-invokes the model when it exits and bills a session everyone believed was over - a money bug, not hygiene - and the Bash tool's hard maximum timeout is 600,000 ms. Tier 4 is already past that (2,039,733-2,266,897 ms across five artefacts), which is exactly why it is not a per-PR gate. Tier 3 is the LAST TIER THAT CAN BE WAITED ON IN ONE CALL and that property is what makes it a usable PR gate. Budget at 80 pct of the ceiling, reserving 20 pct so the assertion fires BEFORE the harness backgrounds the run and hides it. NOT RED ON ARRIVAL: today's loaded figure is 355,533 ms = 74 pct, idle-equivalent 340,506 ms = 71 pct, headroom ~124,500 ms - the next 60-80 s check keeps it green, the one after that turns it red. That is precisely the signal LF-237 wants while avoiding LF-224's trap that a check red on arrival gets disabled rather than fixed. SHIP IT NON-ASSERTING FIRST: adding 3: 480_000.0 to TIER_BUDGET_MS is one line and immediately prints the budget line on every tier-3 run; VERIFIED that nothing in the repo passes --budget, so the number costs zero risk and starts a trend line today. DO NOT add --budget to gate.yml's tier-3 step yet - no CI tier-3 duration exists in any artefact, every artefact is the local 16-core WSL2 box, and binding a number against an unmeasured GitHub container is the same error in a new place. gate.yml already uploads gate-tier3.json as an artifact; measure from that first. ALSO WORTH KNOWING: tier 3 did not DRIFT, it absorbed two discrete checks, so a per-check tier-3 ceiling (~90 s) would have named scenario lf226-fallback at 75.8 s on the day it arrived where a wall-clock total names it three checks later. The total matches --budget's existing mechanism so ship that, but the per-check ceiling is the better diagnostic
- `med` LF-242 restorer.range is dead data in BOTH rules implementations. Sim.capacity_now() (sim/engine.py:608) adds effect_value for every online restorer with no distance test at all, and scripts/anchor_sim.gd mirrors it; the value 1.0 in data/towers.json is never read. PROVED by grading anchor-20 standard with restorer.range at 1.0 and at 40.0 -- by_difficulty identical field for field (tools/range_derive.py --selftest asserts this, and the same assertion returns False when pointed at pulse-turret, so it discriminates). CONSEQUENCE for BAL-04: the range derivation cannot scale it, because scaling nothing is nothing; it is excluded via INERT_RANGE_IDS with the measurement as the reason. THE REAL QUESTION is whether a restorer SHOULD have a radius -- decision 031 prices it as a slot plus a draw for capacity back, and a global effect makes its position free, which is the only emplacement in the game whose siting does not matter. Either give capacity_now() the distance test both engines already have (_covered_by), which is a rules change and needs parity, or delete the range key from the row and say in the note that placement is free. Do not leave a field that reads as tuning and is not.
- `med` LF-246 Authored slot POSITIONS are vestigial since PLC-04, so LF-185's re-siting premise and LF-234's 'the level's slot budget is the limit' framing no longer describe the game. The COUNT still binds; the positions do not. EVIDENCE, all in the tree: build_candidate_lattice()'s own docstring says it outright -- 'the grader's candidate set is now a property of the BOARD, not of the anchor's authored slots ... the grader was reasoning about a game that had stopped existing'. _slot_priority() is marked RETAINED FOR ONE CALLER, NOT USED BY THE GRADER and survives only so tools/analysis_lf014.py can reproduce decision 022's numbers. The player builds through the free cursor (anchor_view.gd:1546) and anchor_view.gd:1532 states free placement has no unoccupied-slot check. The only surviving consumers of the positions are DEV paths: anchor_view.gd:346 (--autoplay's autobuild pick) and main.gd:452 (the --build CLI hook). What DOES still bind is the count, through _effective_cap()'s fallback to len(slots), which 23 of 24 anchors take. CONSEQUENCE 1: sim/coverage.py's presence, lane and site-eff columns and verdict()'s '(a) siting' label are all computed over authored slots and therefore describe vestigial data, while uptime, dmg-share, near-zero and board_presence_ceiling come from real lattice placements and remain valid. A verdict of '(a) siting: authored slots reach 52 pct of what this board's slot budget admits' is not a statement about anything a player can experience. CONSEQUENCE 2: LF-185's headline result -- whole-game near-zero 12.71 pct -> 9.53 pct from re-siting 21 slots -- was measured before the grader stopped reading slot positions, so it cannot be reproduced now and must not be cited as evidence that re-siting works. CONSEQUENCE 3: docs/STATE.md's priority 2 (act 1's presence ceilings, anchors 02-05 at 68.3/65.3/50.1/41.8 pct) needs re-reading: board_presence_ceiling is real and low, but site-eff 0.36-0.47 on anchors 03/04/05 is not a defect to fix. THE WORK is to split coverage.py's report into board-real and authored-vestigial halves, or delete the authored half, and to decide whether anchors should keep authoring slot arrays at all now that only their length is read -- decision 063 already made max_emplacements the intended denominator.
- `med` LF-249 At 200 percent interface scale the undocked threat overlay covers the instrument column rightmost 20 px. Cosmetic. The covered strip is the column padding and scrollbar gutter, not text, and it is deliberate and documented at scripts/hud.gd:854-858 -- the alternative placement pushed 12 px of the threat panel own scrollbar off the right edge of the viewport, which is worse because a scrollbar the player cannot reach is a control they cannot use. From LF-247 and decision 085. Filed because the LF-247 commit message claimed it was already filed and it was not; the chronicler caught that while checking the brief against the tree. No action needed unless someone reports it in play.
- `med` LF-251 game renders cannot tell a board of dampers from a board of turrets, and the LF-236 fix proves it with a byte-identical result line. MEASURED across the fix in one session: before, tools/shot.py anchor-24 --facings returned 24 FACE lines all anchor_damper (12 emplacements, damage 0); after, 24 FACE lines all pulse_turret (12 emplacements, the game main weapon). The board changed completely. tools/check.py game renders reported coverage 0.95, 114 tones BEFORE and coverage 0.95, 114 tones AFTER -- the same two numbers to the digit. So the check that three captures of the running game rest on is insensitive to what is standing on the board. WHAT IT ACTUALLY MEASURES is that the frame is not blank and carries enough distinct colour: coverage is the fraction of non-background pixels and tones is the count of distinct quantised colours, both dominated by terrain, panels and text rather than by emplacements. That is a real thing to check -- it is what caught blank frames and flat-grey atlas regressions -- but it must not be read as evidence about the board, and its own message line invites exactly that reading. THE WORK: either give game renders an assertion about the BOARD (emplacement count and distinct emplacement ids from the same facing dump --facings already prints, which costs nothing extra because the capture is already running), or reword the message so it says frame is non-blank with N tones and stops implying more. Prefer the former: a capture that already knows what it drew should say so. Fifth instance this session of a check that reports on something other than what its name implies -- see LF-243 the grade table cannot see difficulty dissolution, LF-244 six verbs never dispatched, LF-246 coverage.py reporting on vestigial slots, LF-247 the accessibility audit over a 4 px playfield.
- `med` LF-252 docs/chronicle/chronicle.json records an image file name and caption but never the command that produced the frame, so the journal cannot be queried for captures affected by a capture-time defect. Surfaced by LF-236/LF-250: --autoplay built only anchor-damper on every anchor from anchor-10 on, so every --autoplay screenshot of an act-2 or act-3 anchor in the journal shows a board with no damage on it -- and there is no way to enumerate which entries those are. The affected captions stand uncorrected, which is right under append-only, but a reader has no way to know which ones to read carefully. THE FIX is a command field on the image block in chronicle.json, written by whoever adds the entry and rendered as a small caption line or a title attribute, so a future capture-time defect can be traced to the frames it touched with a grep instead of a guess. Cheap, additive, and no existing entry needs to change -- an absent field just means unrecorded. tools/chronicle.py plus the schema if one covers chronicle.json. Raised by the chronicler agent while writing entry 65, which is also the right precedent: it declined to guess which entries were affected and said so in the entry instead.
- `med` LF-254 hard does not bite on anchor-08 or anchor-15: identical win share to standard on both. MEASURED with the win-share metric decision 086 introduced -- anchor-08 is 3 of 14 on standard and 3 of 14 on hard, anchor-15 is 6 of 13 and 6 of 13. Decision 086 deliberately asserts the falls-strictly rule on the TOP tier only and reports the middle ones, because a check red on arrival gets disabled rather than fixed (LF-224); these two anchors are exactly why it is reported rather than asserted. sim/run.py names them on every full grade and tools/session.py writes them under docs/STATE.md grade table on every wrap, so this cannot go quiet. THE WORK is a balance pass on the two anchors, not a change to the rule: hard multiplies enemy hp by 1.35 and cuts bounty to 0.90, and on these two that buys nothing -- the same six or three builds clear both tiers. Fix inside BAL-04 re-grade and then consider promoting the rule from the top tier to every tier, which is the end state decision 086 wants and cannot have today.
- `med` LF-259 Upgrading is a standard-difficulty strategy that collapses as difficulty rises, and it is not obvious that is intended. MEASURED with the upgrade-ladder policy landed by LF-245, whose preference is byte-identical to cheap-mass so every difference is attributable to upgrading alone: on standard the ladder wins 11 of 24 anchors against cheap-mass 9; on hard 5 against 9; on brutal ZERO against 5. It wins 16 of 72 anchor-by-difficulty cells overall against cheap-mass 23 -- so upgrading is a NET NEGATIVE for the campaign as a whole -- while opening 9 cells cheap-mass cannot reach at all, every one of them on standard or hard. THE MECHANISM is visible on anchor-04 standard: upgrading raises an emplacement draw, so the ladder can afford six turrets and two relays where cheap-mass affords seven turrets and one relay, and cheap-mass wins while the ladder loses. Upgrade depth is bought with board WIDTH. And the harder tiers want width: brutal multiplies enemy hp by 1.55 and cuts bounty to 0.80, so the tier that most needs more guns is the tier where you can least afford to make existing ones better. The counter-example is anchor-01 hard, where a FOUR-emplacement upgraded board beats the five-emplacement unupgraded one -- depth substituting for width, on the one anchor small enough for it. THE QUESTION FOR BAL-04, and it is a design question not a tuning one: should the upgrade be a beginner option that stops working, or should upgrade cost/draw be priced so it stays a live choice at the top difficulty? THE MECHANISM IS NOT ESTABLISHED and the obvious explanation is measurably WRONG: an upgrade does raise draw (pulse-turret 12 -> 18 MW) but it raises damage more (9 -> 14), so damage PER MEGAWATT improves slightly on upgrade, 0.75 -> 0.78. Where it loses is money: 190 buys one upgraded turret for 14 damage at 18 MW, while 200 buys two turrets for 18 damage at 24 MW. Since money is the abundant resource here (55-62 pct unspent) and power is the scarce one, upgrading should be attractive on that arithmetic and is not -- so the real cause is likelier to be SEQUENCING (the ladder spends on upgrades at a fixed time, and is then short at the moment its next emplacement becomes affordable, taking a cheaper one in preference order instead) than a draw penalty. That is a hypothesis, not a measurement. Settle it before pricing anything. Note LF-245 landed the policy without answering this deliberately -- the instrument first, the tuning inside BAL-04 re-grade.
- `low` LF-086 THEATRE SCALE 11. Per-node power grid, so supply is regional rather than one global capacity_mw. Losing a flank should cost the guns on that flank. Large rules change; check first whether it can be expressed as data plus an existing effect type before adding a new rule shape
- `low` LF-091 THEATRE SCALE 16. Turret animation: traverse, recoil and reload frames. A turret that visibly turns and recoils sells mass better than any particle effect. Frames are cheap in the atlas and the pivot is already measured, but this multiplies cell count so it interacts with the atlas page budget
- `low` LF-093 THEATRE SCALE 18. Dynamic score and battlefield mix responding to wave intensity, so pressure is audible before it is visible. The audio director already ducks music on brownout; this extends that idea to the whole fight
- `low` LF-159 data/scenarios/abilities.json cannot exercise Threshold Surge's 1.5-tile pushback: every enemy in anchor-01's wave 1 (Warden Drone, <=40 hp) dies outright to the falloff floor's own minimum damage (130*0.35=45.5), and pushback only applies to a unit that survives the hit (anchor_sim.gd fire_surge()). Needs a scenario against a tougher early enemy (or a different anchor/wave) that lands a survivable hit, asserting sim.units.<i>.dist decreases by exactly 1.5 (clamped at 0) rather than only hp/alive. Found verifying PRC-12.
- `low` LF-174 Twelve of the sixteen zero-coverage scripts PRC-18's audit named remain at zero: pause_menu.gd, audio_director.gd (brownout ducking), ui_theme.gd (Ui autoload), cli_args.gd, dialog_view.gd (partially covered as a side effect of gamepad_build.json/abilities.json), abilities.gd (covered via abilities.json)
- `low` LF-256 anchor-21 passes decision 086 falls-strictly rule on a technicality and is the first anchor it will name. MEASURED: anchor-21 grades 4 of 15 on standard and 4 of 16 on brutal -- the SAME absolute count of distinct winning builds, passing only because brutal happened to try one more build. Win-share margin is +1.7 points, against a campaign median around +11 and a next-tightest of +6.7 (anchor-23). The count-based form of the same rule (count(brutal) < count(standard)) fails on anchor-21 and on no other shipped anchor. Recorded so that when BAL-04 loosens anything and grade verdict goes red on anchor-21, it is a known thin margin and not a surprise -- and so nobody weakens the rule to accommodate it. Related: LF-220 already records anchor-02 brutal narrowing in the LF-185 pass, and this is the same class of note.
- `low` LF-258 sim/run.py --json silently drops each report runs unless --detail is also passed, so anything computing a per-run statistic off the JSON gets nothing and says nothing. FOUND while wiring decision 087 overdraw figure into tools/session.py: campaign_discipline() needs per-run brownout_fraction and won, main() --json path filters with {k: v for k, v in r.items() if k != runs or args.detail}, and session.py had been calling --json --jobs 0 with no --detail for as long as it has existed. The function now returns {} rather than raising and the grade verdict selftest drives BOTH halves of that guard -- and the second half was DEAD when written: deleting it left the selftest green, which is how it earned a case. session.py now passes --detail; the cost is about 700 KiB more through the pipe and zero extra grading, because the runs already exist. WHAT IS LEFT: the flag is a trap for the next caller, not just for this one. tools/check.py anchor grades calls --json without --detail and is unaffected only because it reads per-anchor ok. Options are to make --json always include runs (simplest, but it is a 700 KiB default for every caller), or to have --json without --detail emit runs: null rather than omitting the key so a consumer sees an explicit absence instead of a missing attribute. Prefer the second. Low priority: the one live consumer is fixed and guarded.
- `low` LF-260 A policy list divergence between the two rule engines costs 15 minutes to discover, and a name comparison would cost milliseconds. A grading policy is authored in TWO places that must move together: standard_policies() in sim/engine.py and _policies() in scripts/test/parity.gd. VERIFIED THIS IS NOT A SILENT HOLE -- I went looking for one and there is none. tools/test_parity.py _two_way_diffs() computes set(py) - set(gd) and reports "present in python, absent from godot"; _three_way_diffs() reports "present in <sides> only" for any key not in all three. So a missing or extra policy FAILS parity and is named. THE COST IS WALL CLOCK: you learn it after 1,512 simulations through both engines, about 15 minutes locally per platform leg, and on a fresh digest the Windows leg runs too. THE WORK is a tier-1 check in the same family as safe operations and rules autoloads -- pure text analysis, no Godot: extract the policy name literals from parity.gd (they are the first argument to mk.call / mk_core.call / mk_scheduled.call / mk_veterancy.call) and compare the ordered list against [p.name for p in standard_policies(sorted(all tower ids))]. Assert equal lists, in order, because order is load-bearing -- test_parity.py keys rows on (anchor, policy, difficulty) and sim/run.py distinct-build counting is order-independent but the parity harness is not. LOW PRIORITY: parity already tells the truth, this only tells it sooner. Filed while landing LF-245, which added the 21st policy and therefore touched both files.

### Recent commits

```
a7c3773 feat(balance): the grader learns to upgrade, and upgrading turns out to be a standard-difficulty strategy (#160)
b4ea598 docs(balance): three of BAL-04's nine acceptance criteria were failed by the baseline they describe (#159)
54c666b fix(balance): "indiscipline wins" was an artefact of which policies overdraw, and the price stays (#158)
2940bfc feat(balance): the grade table could not see brutal dissolve, and the bound it wanted is not there (#157)
3b3b123 docs(state): wrap — four PRs, and five times the instrument was the thing that was wrong (#156)
57f4f11 fix(engine): autoplay built twelve dampers and the render check reported the same numbers either way (#155)
a41e997 fix(ui): the playfield was 4 px wide at 200% and every accessibility check passed (#154)
1e59d71 test(gate): the six verbs nobody graded are the six with a key bound to them (#153)
```

<!-- END AUTO -->
