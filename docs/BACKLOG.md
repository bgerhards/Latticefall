# Backlog

*Generated by `tools/backlog.py`. Do not hand-edit — regenerate it.*

2 open · 43 closed

## med

| | ID | Area | Kind | Item |
|---|---|---|---|---|
| ` ` | LF-044 | design | polish | Act II/III density: decision 047 removed the blocker, now spend it — re-author Act II/III wave composition for more, lighter units at constant wave leak_cost, then re-sweep anchors 09-24 |
| ` ` | LF-045 | engine | feature | The instrument column cannot reflow, so interface scale is capped at 125%. content_scale_factor divides the 1920x1080 design space, and the column needs ~847px of height at anchor-24 (nine emplacements, eight reserved datasheet rows) — 150% leaves 720 and clips the SELL/UPGRADE/power controls. WCAG 2.1 SC 1.4.4 asks for 200%. Needs a column that can reflow (scroll, collapse the datasheet to the selected tower's rows, or move the build bar out of the column) rather than a larger multiplier |

---

## Closed

| ID | Area | Item | Outcome |
|---|---|---|---|
| LF-043 | audio | debris_settle has no CC0 candidate — OpenGameArt's sound corpus has nothing whose filename reads as falling debris. Needs different queries, --commons, or a recording | done — Not sourceable: the entire CC0 sound corpus offers a shovel, a splash and a sand spell for this. Synthesized instead — debris is a stochastic cloud of tiny impacts, which is what synthesis is good at, so decision 009 had it on the wrong side of its own boundary. Rebuilds byte-identical. Plays under warden_death for units of 150 HP or more. Decision 041. |
| LF-042 | engine | Window title and export name are "Defend-Claude" (the repo name), not LATTICEFALL — project.godot config/name reaches the player | done — config/name is Latticefall. Godot derives user:// from it, so Progress.load_state() adopts a pre-rename save once and prints the path it took. Verified against a real save with anchor-01 cleared: clear survived, second boot did not re-adopt, case-mismatch warning gone. Decision 040. |
| LF-041 | engine | Godot warns on every boot that user://progress.json is opened as Defend-Claude but stored as defend-claude — a case mismatch that will not open on a case-sensitive export target | dropped — Not a repo defect. project.godot is config/name="Defend-Claude"; the on-disk user dir is lowercase because it was created under an earlier name, and macOS resolves it case-insensitively. A fresh machine or a case-sensitive export creates Defend-Claude and never mismatches. Local artifact only. |
| LF-040 | engine | Build bar overflows the left column and covers board slots from Act II on — 9 unlocked emplacements is 1182px of buttons across a 1920px viewport | done — Build bar is a two-column grid inside the 330px instrument column; the inspector is placed against the bar's measured height, so it tracks 1 row on anchor-01 and 5 on anchor-24. Verified by screenshot at both extremes. |
| LF-039 | engine | Between-anchor flow is menu-only: no post-debrief 'next anchor' button, no act transition screen, no way to replay from the debrief | done — closed at c048141 |
| LF-038 | design | Act II waves are thin (2-5 units per type) and lives run high (16-24) because shielded heavies and drain eat the whole difficulty budget — consider a mid-cost anti-armour emplacement or a lance/mortar draw cut so volume can come back | done — Half solved by decision 044: the cause was the sweep's scorer, not the content. It scored clean cells by distinct winning builds alone, so more lives always scored higher and the loosest cell always won. Robustness is now a threshold (ROBUST_ENOUGH=8), with density and tightness deciding beyond it. Seven anchors tightened — 18/19 from 30 lives to 22, 22 from 32 to 26, 24 from 44 to 40; Act III mean 31.8 -> 27.5. Density did NOT move and continues as LF-044. |
| LF-037 | data | Four build slots sit just outside the shortest weapon's range on anchors 03/04/05 | done — closed at c048141 |
| LF-036 | art | No Sable Reach emplacement or sprite exists for Act II | done — closed at 3d01481 |
| LF-035 | story | Write dialog for anchors 09-24 | done — closed at 8c05a5c |
| LF-034 | data | anchor-06 Hard Currency is authored but does not grade clean — parked | done — closed at 6c78fc1 |
| LF-033 | design | Ion lance is strictly dominated: worse damage per megawatt than the pulse turret even against armour | dropped — — |
| LF-032 | design | Shield wall is overpriced: 40 MW for a 0.45x slow in radius 2.0 is never worth building | done — closed at 5f13193 |
| LF-031 | art | Arc node and anchor ring emissives saturate to white in the additive glow layer | done — closed at ccf0e4e |
| LF-030 | art | No sprites for scan-relay, shield-wall or ion-lance — they draw as placeholder polygons | done — closed at 8a9ec0a |
| LF-029 | tooling | Sim state diverged twice at a fixed frame while the sprite import cache was stale | done — closed at 9d9ff55 |
| LF-028 | tooling | Frame pacing not reproducible under --fixed-fps | dropped — — |
| LF-027 | art | Sprite pivot ignores the render camera height_bias, so every sprite draws ~48px above its tile | done — closed at 7ced6e6 |
| LF-026 | engine | HUD and dialog build their Control widgets in code rather than being authored in the scene | dropped — Superseded by decision 043. Every HUD panel is now sized from content — build grid from the unlock count, inspector from the tallest datasheet measured against the font's line height, threat panel from the busiest wave. A .tscn cannot express those heights, so authoring it would mean keeping all the layout code and adding a scene that disagrees with it. Two sources of truth for one geometry. |
| LF-025 | tooling | Editor preview shows flat-colour tiles until the project is reloaded, because the Sprites autoload is only instantiated in-editor at startup | done — anchor_view._sprite_lib() builds its own sprites.gd instance when the autoload is absent, which is the editor case. sprites.gd's manifest load moved from _ready() into load_library() so it can be called explicitly — the same fix anchor_data.gd already makes for Content. Verified standalone: 24 assets, atlas 2 pages, returns AtlasTexture with no autoload present. |
| LF-024 | art | No contact shadow under units or emplacements | done — closed at 7ced6e6 |
| LF-023 | art | Board tiles read light grey against the dark UI | done — closed at 7ced6e6 |
| LF-022 | art | Turret muzzle emissive clips to white | done — closed at 7ced6e6 |
| LF-021 | art | Anchor ring, heavy and mote need silhouette pass at 100% zoom | done — closed at c048141 |
| LF-020 | art | Sprite emissives still over-driven; drone eye reads white not red | done — closed at 7ced6e6 |
| LF-019 | engine | Emplacements cannot be sold or upgraded | done — closed at c048141 |
| LF-018 | tooling | Unfocused Godot window is throttled — verification must use --fixed-fps | dropped — Superseded by decision 043. A platform fact, not a task: it cannot be fixed, only known. Recorded in CLAUDE.md and STATE's trap list, and enforced by every verification command passing --fixed-fps. |
| LF-017 | engine | No main menu, level select, or between-anchor flow | done — closed at 8c05a5c |
| LF-016 | art | Sprites: board and units are placeholder polygons | done — sprites wired; board, emplacements, units and additive glow all rendered |
| LF-015 | design | Rebalance anchors 02-24 as they are authored | done — closed at c048141 |
| LF-014 | design | Brownout is never a rational choice with homogeneous towers | done — closed at 8cbe408 |
| LF-013 | build | git-lfs not installed | done — installed; masters later removed from git per decision 012 |
| LF-012 | art | Blender emissive/bloom unwired | done — resolved: separate glow layer, decision 007 |
| LF-011 | audio | A2-CMB seam 0.00749 is second-highest of the set | done — user auditioned all loops, judged fine |
| LF-010 | engine | Gamepad support deferred until content-complete | done — Thirteen lf_* actions bound to keyboard and gamepad together, generated idempotently by tools/godot/setup_input.gd rather than hand-written into project.godot. Board cursor steps slot to slot, scored in screen space. Dialog consumes lf_confirm so one gamepad button does not both advance a line and build. Verified by screenshot with --cursor. Decision 042. |
| LF-009 | engine | No save system or meta-progression | done — closed at 8c05a5c |
| LF-008 | build | IBM Plex Mono/Sans Condensed not vendored | done — closed at 8c05a5c |
| LF-007 | audio | Godot .import settings for music loop flags not generated | done — closed at 8c05a5c |
| LF-006 | story | Write dialog for all 24 anchors | dropped — — |
| LF-005 | audio | Source the 12 organic CC0 SFX | done — 11 of 12 promoted. CC0-only confirmed as decision 038; twelve cues enumerated in fetch_cc0.py; 26 candidates sourced, licence-verified and auditioned by the owner (12 keep, 14 drop); promoted by promote_cc0.py with cuts baked into the assets, RMS-matched per decision 010, and logged in SOURCES.md with URL/author/licence/SHA-256. comms_squelch and comms_close are wired into dialog. debris_settle found no candidate and continues as LF-043. |
| LF-004 | art | No sprite atlas packer | done — Fixed-grid atlas, one page per pass, 192 loose textures -> 2. 8.7 MB -> 4.0 MB. sprites.gd serves AtlasTexture regions and still falls back to loose files when unpacked. Gate check 'sprite atlas' hashes every render and fails on a stale page — verified by perturbing one pixel. Decision 039. |
| LF-003 | art | Productionize Blender render pipeline from validation script | done — tools/blender/render.py: 9 assets, 4 yaws, albedo+glow, self-calibrating |
| LF-002 | engine | Headless combat sim not written | done — sim/ written; check.py sim determinism now passes |
| LF-001 | engine | Godot: no gameplay code exists yet — engine layer is empty | done — anchor-01 playable end to end; parity-gated against sim |
